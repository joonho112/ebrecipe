% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-wrappers.R
\name{plot_decision}
\alias{plot_decision}
\title{Plot a compact EB decision dashboard}
\usage{
plot_decision(
  observed,
  grid,
  classification = NULL,
  lambda = 0.5,
  fdr_level = 0.05,
  selection_share = 0.2,
  characteristic,
  p_binwidth = NULL,
  surface_size = 0.72,
  observed_size = 3.1,
  combine = c("patchwork", "list"),
  title = "EB decision rules"
)
}
\arguments{
\item{observed}{Observed posterior data frame or \code{eb_posterior}/\code{eb_fit}
object.}

\item{grid}{Posterior decision-surface grid data frame.}

\item{classification}{Optional \code{eb_classification}-like object containing
\code{p_values}, \code{q_values}, and \code{pi0}.}

\item{lambda}{Storey threshold used when \code{classification} is not supplied.}

\item{fdr_level}{FDR threshold used for selected-count metadata.}

\item{selection_share}{Matched selection share for the frontier.}

\item{characteristic}{Length-one label for the empirical characteristic.}

\item{p_binwidth}{Optional p-value histogram bin width.}

\item{surface_size}{Point size for grid/surface points.}

\item{observed_size}{Point size for observed points.}

\item{combine}{\code{"patchwork"} returns a combined dashboard; \code{"list"} returns
named component plots.}

\item{title}{Optional dashboard title.}
}
\value{
A patchwork dashboard or a named list of ggplot objects.
}
\description{
Combines the p-value distribution and decision frontier into a two-row
dashboard for comparing q-value and posterior-mean selection rules.
}
\details{
The decision dashboard is a targetless workflow view. For exact Lane A
Figure 04-03 parity, call \code{\link[=plot_fdr_histogram]{plot_fdr_histogram()}} and
\code{\link[=plot_decision_frontier]{plot_decision_frontier()}} directly with protected target IDs and matching
source receipts.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
observed <- data.frame(
  theta_hat = c(0.01, 0.03, 0.05, 0.07),
  s = c(0.02, 0.02, 0.03, 0.04),
  theta_star = c(0.015, 0.028, 0.045, 0.055),
  firm_id = letters[1:4]
)
grid <- expand.grid(
  theta_hat = seq(-0.01, 0.08, by = 0.01),
  s = seq(0.015, 0.05, length.out = 8)
)
grid$theta_star <- 0.6 * grid$theta_hat
grid$theta_star_lin <- 0.55 * grid$theta_hat
grid$theta_star_lin_alt <- 0.58 * grid$theta_hat
grid$p_value <- stats::pnorm(-(grid$theta_hat / grid$s))
panels <- plot_decision(
  observed,
  grid,
  characteristic = "white",
  selection_share = 0.50,
  combine = "list"
)
names(panels)
\dontshow{\}) # examplesIf}
}
