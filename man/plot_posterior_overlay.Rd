% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-posterior-overlay.R
\name{plot_posterior_overlay}
\alias{plot_posterior_overlay}
\title{Plot a companion-style posterior shrinkage overlay}
\usage{
plot_posterior_overlay(
  posterior,
  density = NULL,
  characteristic,
  binwidth = NULL,
  origin = NULL,
  trim = TRUE,
  target_id = NULL,
  source_receipt = NULL,
  validation_mode = c("strict", "exploratory", "none")
)
}
\arguments{
\item{posterior}{A posterior data frame or \code{eb_posterior}/\code{eb_fit} object.
Companion oracle CSVs with columns \code{theta_hat}, \code{s}, \code{theta_star}, and
optional comparison columns are accepted, as are unnamed ten-column CSV
imports.}

\item{density}{Optional theta-scale mixing-density data frame or \code{eb_prior}
object. Companion oracle CSVs with columns \code{x}, \code{density}, \code{sample_mean},
\code{model_mean}, \code{bias_corrected_sd}, and \code{model_sd} are accepted, as are
unnamed six-column CSV imports.}

\item{characteristic}{Length-one label for the empirical characteristic being
plotted, such as \code{"white"} or \code{"male"}. The companion aliases \code{"race"}
for \code{"white"} and \code{"gender"} for \code{"male"} are also accepted.}

\item{binwidth}{Histogram bin width. When \code{NULL}, companion theta-scale
defaults are used: \code{0.005} for race/white plots and \code{0.01} for
gender/male plots.}

\item{origin}{Histogram bin origin/boundary. When \code{NULL}, the companion
default \code{0} is used.}

\item{trim}{Logical. When \code{TRUE}, apply the companion theta-scale density
trimming rules (\code{x <= 0.15} for race/white and \code{abs(x) <= 0.2} for
gender/male).}

\item{target_id}{Optional internal replication target identifier.}

\item{source_receipt}{Optional companion parity source receipt. In strict
mode, protected companion targets such as \code{posterior_white} must provide
the matching receipt so layer row counts and target metadata are checked.}

\item{validation_mode}{Target validation mode. The default \code{"strict"}
requires receipts for protected companion targets, \code{"exploratory"} checks
target metadata when possible without requiring a receipt, and \code{"none"}
disables target validation.}
}
\value{
A \code{ggplot} object. The internal \code{eb_figure_data} object used to
build the plot is stored in \code{attr(plot, "eb_figure_data")}.
}
\description{
Draws the Walters (2024) companion overlay of raw unit estimates,
nonparametric posterior means, and the deconvolved original-scale mixing
density \code{g(theta)}. The plot is designed for the Figure 04-01 posterior
overlay targets while remaining a general ggplot wrapper around
\code{eb_figure_data}.
}
\details{
The companion Figure 04-01 targets are \code{posterior_white} and
\code{posterior_male}. The raw-estimate histogram is drawn from \code{theta_hat}, the
posterior histogram is drawn from \code{theta_star}, and the optional black curve
is the theta-scale mixing density \code{g(theta)}.

Exact Lane A companion examples require both \code{target_id} and
\code{source_receipt} so the helper can validate source assets and row counts.
Live workflow overlays should omit protected target IDs. See
\code{vignette("visualization", package = "ebrecipe")} for receipt-backed
examples.
}
\examples{
\dontrun{
# plot_posterior_overlay() expects a theta-scale `density`. The live
# `eb_deconvolve()` output is on the residual (r) scale; convert it via
# `eb_change_of_variables(prior, s, psi_1, psi_2, model)` first, or pass
# the companion theta-scale fixture (see the a5 visualization cookbook).
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data("krw_firms", package = "ebrecipe")
  est <- eb_input(krw_firms$theta_hat_race, krw_firms$se_race)
  prior <- eb_deconvolve(est, grid_size = 80, penalty = "none")
  post <- eb_shrink(est, prior)
  # Convert prior to theta scale (multiplicative example):
  prior_theta <- eb_change_of_variables(
    prior, s = mean(krw_firms$se_race),
    psi_1 = prior$spline_info$psi_1,
    psi_2 = prior$spline_info$psi_2,
    model = "multiplicative"
  )
  plot_posterior_overlay(post, density = prior_theta,
                         characteristic = "white")
}
}
}
