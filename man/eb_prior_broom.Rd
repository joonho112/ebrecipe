% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R
\name{autoplot.eb_prior}
\alias{autoplot.eb_prior}
\alias{eb_prior_broom}
\alias{tidy.eb_prior}
\alias{glance.eb_prior}
\title{Broom and ggplot2 methods for \code{eb_prior} objects}
\usage{
autoplot.eb_prior(x, ...)

tidy.eb_prior(x, ...)

glance.eb_prior(x, ...)
}
\arguments{
\item{x}{An \code{eb_prior} object.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.

A data frame with \code{term}, \code{estimate}, and \code{std.error}.

A one-row data frame.
}
\description{
Visualizes the fitted prior. For the linear path (\code{tidy()} returns
\code{mu_hat}/\code{sigma_theta} rows) draws a two-bar column chart of the
hyperparameter point estimates. For the nonparametric path (rows named
\verb{support_<i>}) draws the estimated mixing density as a line over the
actual support grid (\code{x$support}).

\code{tidy()} returns a long data frame describing the fitted prior. For the
linear path (\code{method = "normal"}, with \code{hyperparameters} populated) it
stacks rows for \code{mu_hat} and \code{sigma_theta}. For the nonparametric path
(any prior with a non-empty \code{support} and \code{density} grid) it emits one row
per support point (\verb{support_<i>}) with the corresponding density value as
\code{estimate}. Goodness-of-fit fields (\code{log_likelihood}, \code{penalty_value})
surface via \code{glance()} instead.

Returns a one-row data frame with \code{method}, \code{n_support} (length of the
support grid), \code{log_likelihood}, \code{penalty_value}, and the linear-EB
hyperparameters \code{mu_hat} and \code{sigma_theta} when populated.
}
