% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mixing-distribution.R
\name{plot_mixing_distribution}
\alias{plot_mixing_distribution}
\title{Plot a companion-style EB mixing distribution}
\usage{
plot_mixing_distribution(
  data,
  characteristic,
  scale = c("r", "theta"),
  estimates = NULL,
  binwidth = NULL,
  origin = NULL,
  trim = TRUE,
  annotate = TRUE,
  target_id = NULL,
  source_receipt = NULL,
  validation_mode = c("strict", "exploratory", "none")
)
}
\arguments{
\item{data}{A mixing-density data frame or \code{eb_prior} object. Companion
oracle CSVs with columns \code{x}, \code{density}, \code{sample_mean}, \code{model_mean},
\code{bias_corrected_sd}, and \code{model_sd} are accepted, as are unnamed six-column
CSV imports.}

\item{characteristic}{Length-one label for the empirical characteristic being
plotted, such as \code{"white"} or \code{"male"}. The companion aliases \code{"race"}
for \code{"white"} and \code{"gender"} for \code{"male"} are also accepted. For
residual-scale estimates this also selects the companion standardization
formula.}

\item{scale}{Plot scale. Use \code{"r"} for the standardized residual scale and
\code{"theta"} for the original contact-penalty scale.}

\item{estimates}{Optional unit-level estimates used for the histogram layer.
Numeric vectors are accepted directly as already being on the requested
plot scale. Theta-scale data frames use \code{theta_hat} or \code{estimate}; unnamed
companion CSV imports may use \code{V1}. Residual-scale data frames must provide
\code{r_hat}/\code{estimate}, or include \code{theta_hat}, \code{s}, \code{psi1}, and \code{psi2}
columns, with underscore variants \code{psi_1} and \code{psi_2} also accepted.}

\item{binwidth}{Histogram bin width. When \code{NULL}, companion defaults are
used: \code{0.2} on the residual scale, \code{0.005} for race/white theta plots, and
\code{0.01} for gender/male theta plots.}

\item{origin}{Histogram bin origin/boundary. When \code{NULL}, the companion
default \code{0} is used.}

\item{trim}{Logical. When \code{TRUE}, apply the companion theta-scale density
trimming rules (\code{x <= 0.15} for race/white and \code{abs(x) <= 0.2} for
gender/male). Residual-scale density curves are not trimmed.}

\item{annotate}{Logical. When \code{TRUE}, add companion moment annotations.
Plain \code{eb_prior} objects do not carry the companion moment columns, so use
\code{annotate = FALSE} unless \code{data} includes those summaries.}

\item{target_id}{Optional internal replication target identifier.}

\item{source_receipt}{Optional companion parity source receipt. In strict
mode, protected companion targets such as \code{g_theta_white} must provide the
matching receipt so row counts and target metadata are checked before
plotting.}

\item{validation_mode}{Target validation mode. The default \code{"strict"}
requires receipts for protected companion targets, \code{"exploratory"} checks
target metadata when possible without requiring a receipt, and \code{"none"}
disables target validation.}
}
\value{
A \code{ggplot} object. The internal \code{eb_figure_data} object used to
build the plot is stored in \code{attr(plot, "eb_figure_data")}.
}
\description{
Draws the deconvolved mixing distribution and, when \code{estimates} is supplied,
overlays the observed-estimate histogram. The function covers both
standardized residual-scale plots \code{g(r)} and original contact-penalty-scale
plots \code{g(theta)} used in Walters (2024) companion Figure 04-01.
}
\details{
The companion Figure 04-01 targets are \code{g_r_white}, \code{g_r_male},
\code{g_theta_white}, and \code{g_theta_male}. Residual-scale plots use the
Walters-style standardization formulas for race/white and gender/male
estimates. Theta-scale plots assume the density has already been
back-transformed onto the contact-penalty scale, for example by
\code{\link[=eb_change_of_variables]{eb_change_of_variables()}} or by a companion oracle CSV.

Exact Lane A companion examples require both \code{target_id} and
\code{source_receipt} so the helper can validate source assets and row counts.
Live workflow plots should omit protected target IDs. See
\code{vignette("visualization", package = "ebrecipe")} for receipt-backed
examples.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data("krw_firms", package = "ebrecipe")
  est <- eb_input(krw_firms$theta_hat_race, krw_firms$se_race)
  prior <- eb_deconvolve(est, grid_size = 80, penalty = "none")
  # eb_deconvolve() returns the prior on the residual (r) scale; ask
  # plot_mixing_distribution() for the matching scale. Use
  # eb_change_of_variables(prior, s, psi_1, psi_2, model) first if you
  # want the theta-scale density.
  plot_mixing_distribution(
    prior,
    characteristic = "white",
    scale = "r",
    estimates = krw_firms$theta_hat_race,
    annotate = FALSE
  )
}
}
}
