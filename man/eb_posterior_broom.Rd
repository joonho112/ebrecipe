% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R, R/fortify.R
\name{autoplot.eb_posterior}
\alias{autoplot.eb_posterior}
\alias{eb_posterior_broom}
\alias{tidy.eb_posterior}
\alias{glance.eb_posterior}
\alias{augment.eb_posterior}
\alias{fortify.eb_posterior}
\title{Broom and ggplot2 methods for \code{eb_posterior} objects}
\usage{
autoplot.eb_posterior(x, ...)

tidy.eb_posterior(x, ...)

glance.eb_posterior(x, ...)

augment.eb_posterior(x, data = NULL, ...)

fortify.eb_posterior(model, data, ...)
}
\arguments{
\item{x}{An \code{eb_posterior} object.}

\item{...}{Forwarded to \code{tidy.eb_posterior()}.}

\item{data}{Unused (kept for ggplot2 fortify generic signature).}

\item{model}{An \code{eb_posterior} object.}
}
\value{
A \code{ggplot} object.

A unit-level data frame with \code{term}, \code{estimate}, \code{std.error},
\code{posterior.mean}, \code{posterior.sd}, \code{shrinkage.weight}, and \code{variance.ratio}.

A one-row data frame.

A data frame with augmented posterior columns.

A \code{data.frame} (the result of \code{tidy.eb_posterior(model, ...)}).
}
\description{
Draws the posterior shrinkage map: pre-shrinkage \code{theta_hat} on the
x-axis vs \code{posterior.mean} on the y-axis, with a dashed \code{y = x}
reference. Points falling away from the diagonal are pulled toward the
prior mean.

\code{tidy()} returns one row per unit. For symmetry with \code{\link[=tidy.eb_fit]{tidy.eb_fit()}},
\code{estimate} carries the posterior mean and \code{std.error} carries the original
observed \code{.s}. Per the documented design decision, both
\code{shrinkage.weight} and \code{variance.ratio} columns are emitted: the linear
path populates \code{shrinkage.weight}; the nonparametric path populates
\code{variance.ratio}; the inactive column carries \code{NA} so consumers can
rbind-stack tidy outputs across fit methods.

Returns a one-row data frame with \code{method}, \code{nobs}, the mean shrinkage
weight (linear path) or mean variance ratio (NP path), and the
posterior-mean range.

\code{augment()} returns the per-unit posterior table joined with the input
\code{data} (when supplied). Columns added: \code{.fitted} (posterior mean),
\code{.resid} (theta_hat - posterior mean), \code{.posterior_sd},
\code{.shrinkage_weight}, \code{.variance_ratio} (dual-column).

Equivalent to \code{as.data.frame(tidy(model))}. The result includes the
dual-column posterior schema (\code{shrinkage.weight} ∪
\code{variance.ratio}).
}
