% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typed-accessors.R
\name{precision_fit}
\alias{precision_fit}
\title{Extract the precision-dependence fit from an EB workflow object}
\usage{
precision_fit(x, ...)
}
\arguments{
\item{x}{An EB workflow object: an \code{eb_estimates}, \code{eb_diagnostic}, or
\code{eb_fit}. Other classes raise a typed-class error from the default
method.}

\item{...}{Method-specific arguments. The \code{eb_diagnostic} method accepts
\code{model = "multiplicative"} or \code{model = "additive"} to select between the
two parametric fits; default is multiplicative if available, else
additive.}
}
\value{
Either:
\describe{
\item{an \code{eb_precision_fit} object}{(once v2.1 wraps the legacy shape) carrying class \code{"eb_precision_fit"} and the same fields as below.}
\item{a v1-shape NLLS list}{with \code{psi_1}, \code{se_psi_1}, \code{psi_2}, \code{se_psi_2}, \code{r_squared}, \code{vcov}, \code{method} -- returned verbatim during the v2.0 transition.}
\item{\code{NULL}}{when no precision fit is attached (e.g. an \code{eb_diagnostic} built with \code{precision_models = character(0)}, or an \code{eb_fit} where standardization was disabled).}
}
}
\description{
v2 typed accessor for the precision-dependence NLLS fit embedded in an
\code{eb_estimates}, \code{eb_diagnostic}, or \code{eb_fit} object. The fit characterises
how estimates depend on their standard errors via either the multiplicative
model \eqn{\hat\theta_j = \exp(\psi_1 + \psi_2 \log s_j) \cdot r_j} or the
additive model \eqn{\hat\theta_j = \psi_0 + s_j^{\psi_2} \cdot r_j}.
Replaces the v1 \code{attr(x, "precision_fit")} pattern with a typed,
class-dispatched accessor.
}
\details{
v2-NEW typed accessor per redesign Step 2.5. Methods are dispatched on
the input class: \code{precision_fit.eb_estimates}, \code{precision_fit.eb_diagnostic},
and \code{precision_fit.eb_fit}. The default method raises a typed-class error.

Walters Ch 2.6 eq. 55 (multiplicative) and Ch 2.7 (additive) define the
\eqn{\psi} parameters returned. R-squared values support the
model-comparison branch of the \code{\link[=eb_diagnose]{eb_diagnose()}} decision tree.
}
\section{v2.0 transitional shape}{

When the underlying object stores the fit as a v1-shape NLLS list
(elements \code{psi_1}, \code{se_psi_1}, \code{psi_2}, \code{se_psi_2}, \code{r_squared},
\code{vcov}, \code{method}), the accessor returns that list verbatim -- the v1
contract is preserved. v2.1 will wrap the legacy shape into a proper
\code{eb_precision_fit} object created via \code{new_eb_precision_fit()}; user
code reading the shared field names (\verb{$psi_1}, \verb{$psi_2}, \verb{$r_squared})
works unchanged across the transition.
}

\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = krw_firms$theta_hat_race,
  s         = krw_firms$se_race,
  unit_id   = krw_firms$firm_id
)
diag <- eb_diagnose(
  est,
  precision_models = c("multiplicative", "additive")
)

fit_mul <- precision_fit(diag, model = "multiplicative")
fit_mul$psi_1
fit_mul$r_squared

# Default selection (multiplicative if available, else additive).
precision_fit(diag)$method

}
\seealso{
\code{\link[=eb_diagnose]{eb_diagnose()}}, \code{\link[=eb_standardize]{eb_standardize()}}, \code{new_eb_precision_fit()},
\code{\link[=selected_units]{selected_units()}},
\code{\link[=tidy.eb_precision_fit]{tidy.eb_precision_fit()}}, \code{\link[=glance.eb_precision_fit]{glance.eb_precision_fit()}}

Other eb_diagnostic: 
\code{\link{eb_diagnose}()}
}
\concept{eb_diagnostic}
