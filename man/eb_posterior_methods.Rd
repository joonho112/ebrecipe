% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-methods.R
\name{eb_posterior_methods}
\alias{eb_posterior_methods}
\alias{print.eb_posterior}
\alias{summary.eb_posterior}
\alias{coef.eb_posterior}
\alias{fitted.eb_posterior}
\alias{residuals.eb_posterior}
\alias{confint.eb_posterior}
\alias{nobs.eb_posterior}
\alias{vcov.eb_posterior}
\alias{as.data.frame.eb_posterior}
\title{Inspect \code{eb_posterior} objects}
\usage{
\method{print}{eb_posterior}(x, ...)

\method{summary}{eb_posterior}(object, ...)

\method{coef}{eb_posterior}(object, ...)

\method{fitted}{eb_posterior}(object, ...)

\method{residuals}{eb_posterior}(object, ...)

\method{confint}{eb_posterior}(object, parm = NULL, level = 0.95, ...)

\method{nobs}{eb_posterior}(object, ...)

\method{vcov}{eb_posterior}(object, ...)

\method{as.data.frame}{eb_posterior}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An \code{eb_posterior} object.}

\item{...}{Unused.}

\item{object}{An \code{eb_posterior} object.}

\item{parm}{Optional subset of units passed to \code{confint()}.}

\item{level}{Confidence level passed to \code{confint()}.}

\item{row.names}{Optional row names passed to \code{as.data.frame()}.}

\item{optional}{Unused standard \code{as.data.frame()} argument.}
}
\value{
\code{summary()} returns an invisible \code{summary.eb} list. \code{coef()},
\code{fitted()}, and \code{residuals()} return named numeric vectors. \code{confint()}
returns a two-column matrix. \code{nobs()} returns the number of units.
\code{vcov()} returns a diagonal variance matrix, possibly containing \code{NA}
values. \code{as.data.frame()} returns the stored posterior table unchanged.
}
\description{
These methods expose the stored posterior summary table.
}
\details{
\itemize{
\item \code{coef()} and \code{fitted()} return posterior means by unit
\item \code{residuals()} returns \code{theta_hat - posterior_mean}
\item \code{confint()} returns stored intervals when available, otherwise a normal
approximation from \code{.posterior_sd}
\item \code{vcov()} returns a diagonal matrix built from \code{.posterior_sd^2} and does
not estimate cross-unit posterior covariance
}
}
\examples{
residual_est <- eb_input(
  theta_hat = c(-0.10, 0.05, 0.20, 0.35),
  s = c(0.20, 0.20, 0.20, 0.20)
)
prior <- eb_deconvolve(
  residual_est,
  penalty = "fixed",
  penalty_value = 0.03,
  characteristic = "male"
)
post <- eb_shrink(residual_est, prior, method = "nonparametric", unstandardize = FALSE)

summary(post)
coef(post)
head(as.data.frame(post))

}
