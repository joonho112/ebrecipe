% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shrink.R
\name{eb_shrink}
\alias{eb_shrink}
\title{Compute posterior shrinkage estimates}
\usage{
eb_shrink(
  estimates,
  prior,
  method = c("nonparametric", "linear"),
  unstandardize = TRUE,
  ...
)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object.}

\item{prior}{An \code{eb_prior} object.}

\item{method}{Shrinkage method. \code{"nonparametric"} uses the supplied prior;
\code{"linear"} uses method-of-moments linear shrinkage from \code{estimates}. The
linear path keeps \code{prior} on the returned object for bookkeeping but
does not otherwise use it.}

\item{unstandardize}{Logical. When \code{TRUE} and
\code{estimates$standardized = TRUE}, uses the standardization metadata
stored in \code{prior$spline_info} to return \code{.theta_hat}, \code{.s}, and
\code{.posterior_mean} on the original \eqn{\theta} scale. When \code{FALSE},
columns remain on the working (typically residual) scale.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
An \code{eb_posterior} object whose \code{posterior} data frame has nine
columns:
\describe{
\item{\code{.unit_id}}{Unit identifier (or \code{seq_along()} if absent).}
\item{\code{.theta_hat}}{Estimate on the output scale (theta scale when \code{unstandardize = TRUE} and inputs are standardized; otherwise the working scale).}
\item{\code{.s}}{Standard error on the matching scale.}
\item{\code{.posterior_mean}}{Posterior mean on the same scale as \code{.theta_hat}.}
\item{\code{.posterior_sd}}{Currently placeholder \code{NA_real_}.}
\item{\code{.shrinkage_weight}}{Linear-path \eqn{w_j \in \eqn{[0, 1]}}; \code{NA_real_} on the NP path.}
\item{\code{.variance_ratio}}{NP-path \eqn{V_j^* / s_j^2} (unclipped); \code{NA_real_} on the linear path.}
\item{\code{.ci_lower}}{Currently placeholder \code{NA_real_}.}
\item{\code{.ci_upper}}{Currently placeholder \code{NA_real_}.}
}
\code{.shrinkage_weight} and \code{.variance_ratio} are MUTUALLY EXCLUSIVE per row.
}
\description{
Combine an \code{eb_estimates} object and an \code{eb_prior} into posterior mean
summaries. The nonparametric path integrates the supplied prior against
the likelihood (Walters Ch 5 eq. 8); the linear path applies closed-form
linear EB shrinkage (Walters Ch 2.4) and ignores \code{prior} in the
calculation. For standardized workflows, \code{unstandardize = TRUE} (default)
returns the posterior on the original \eqn{\theta} scale.
}
\details{
Implements both shrinkage paths from Walters Ch 2.4 (linear closed form)
and Walters Ch 5 eq. 8 (nonparametric posterior). The two paths emit
MUTUALLY EXCLUSIVE shrinkage columns:

\itemize{
\item Linear path: \code{.shrinkage_weight} = \eqn{w_j \in \eqn{[0, 1]}} (data weight per Walters Ch 2.1 eq. 12); \code{.variance_ratio} is \code{NA}. A defensive bound enforces \eqn{w_j \in \eqn{[0, 1]}} and errors on violation (frozen-engine invariant).
\item Nonparametric path: \code{.variance_ratio} = \eqn{V_j^* / s_j^2} computed from the \eqn{J \times M} grid weights, NOT clipped (may exceed 1 in tails for non-Gaussian priors per Worksheet B.1); \code{.shrinkage_weight} is \code{NA}.
}

The dual-column convention is intentional: a single posterior
table can carry either kind of weight, and downstream consumers branch on
which column is non-\code{NA}. Posterior-SD and CI columns (\code{.posterior_sd},
\code{.ci_lower}, \code{.ci_upper}) are currently placeholders filled with \code{NA}.

When \code{unstandardize = FALSE}, output remains on the current working scale;
this is for debugging residual-scale workflows, not user-facing display.
}
\section{Decision tree -- NP vs. linear}{

\itemize{
\item \code{method = "nonparametric"} (default with NP \code{eb_deconvolve} prior): full posterior via integration; emits \code{.variance_ratio} (unbounded; may exceed 1 in tails per Worksheet B.1).
\item \code{method = "linear"}: closed-form linear EB; emits \code{.shrinkage_weight} in \eqn{[0, 1]}.
}
Columns are MUTUALLY EXCLUSIVE per row (dual-column posterior).
}

\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = utils::head(krw_firms$theta_hat_race, 80),
  s = utils::head(krw_firms$se_race, 80)
)

# Linear path -- closed form, fast.
linear_prior <- eb_deconvolve(est, grid_size = 30, penalty = "none")
post_lin <- eb_shrink(est, linear_prior, method = "linear")
head(post_lin$posterior[, c(".theta_hat", ".posterior_mean",
                            ".shrinkage_weight")])

\donttest{
# NP path on standardized inputs (~1-3 s on 80 firms with grid_size = 100).
diag_fit <- eb_diagnose(est, precision_models = "multiplicative")
std_est  <- eb_standardize(est, model = "multiplicative",
                           diagnostic = diag_fit)
prior_r  <- eb_deconvolve(std_est, grid_size = 100, penalty = "none")
post_np  <- eb_shrink(std_est, prior_r, method = "nonparametric",
                      unstandardize = TRUE)
head(post_np$posterior[, c(".theta_hat", ".posterior_mean",
                           ".variance_ratio")])
}

}
\seealso{
\code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_standardize]{eb_standardize()}}, \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}},
\code{\link[=eb_posterior_grid]{eb_posterior_grid()}}, \code{\link[=eb_reliability]{eb_reliability()}}, \code{\link[=eb_mse]{eb_mse()}}

Other eb_posterior: 
\code{\link{eb_mse}()},
\code{\link{eb_posterior_grid}()},
\code{\link{eb_reliability}()},
\code{\link{eb_shrink_conditional}()}
}
\concept{eb_posterior}
