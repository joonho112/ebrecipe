% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-cov.R
\name{eb_change_of_variables}
\alias{eb_change_of_variables}
\title{Transform an r-scale prior to the original theta scale}
\usage{
eb_change_of_variables(
  prior,
  s,
  psi_1,
  psi_2,
  model = c("multiplicative", "additive")
)
}
\arguments{
\item{prior}{An \code{eb_prior} object on the residual scale \code{r}. Priors already
transformed to \code{scale = "theta"} are rejected.}

\item{s}{Full vector of standard errors on the original \eqn{\theta} scale.
The empirical distribution of \code{s} defines the theta-scale pushforward;
changing \code{s} changes the returned prior.}

\item{psi_1}{First precision-dependence parameter \eqn{\hat\psi_1}. In the
additive model this is the additive intercept \eqn{\hat\psi_0} carried
forward from the standardization fit; in the multiplicative model it is
the slope intercept \eqn{\hat\psi_1}.}

\item{psi_2}{Second precision-dependence parameter \eqn{\hat\psi_2} (the
\code{log(s)} coefficient).}

\item{model}{Precision-dependence model; one of \code{"multiplicative"} or
\code{"additive"}.}
}
\value{
An \code{eb_prior} object on the theta scale, with fields:
\describe{
\item{\code{method}}{Inherited from the input r-scale prior.}
\item{\code{alpha}}{Inherited free spline coefficients (carried for bookkeeping; not re-fit on theta scale).}
\item{\code{support}}{Numeric vector (length \code{length(prior$support)}) giving the new \eqn{\theta} grid; strictly increasing.}
\item{\code{density}}{Renormalized theta-scale density.}
\item{\code{V}}{Always \code{NULL} -- the r-scale sandwich VCV is dropped.}
\item{\code{hyperparameters}}{Pushforward summaries: \code{mu} (\eqn{\sum \theta \cdot \mathrm{mass}}), \code{sigma_theta}, \code{sigma_theta_sq} (clipped at 0 for discretization rounding).}
\item{\code{scale}}{Always \code{"theta"}.}
\item{\code{spline_info}}{The original \code{prior$spline_info} augmented with \code{change_of_variables_model} (the chosen mapping) and \code{change_of_variables_n} (= \code{length(s)}).}
}
}
\description{
Maps an r-scale prior produced by \code{\link[=eb_deconvolve]{eb_deconvolve()}} forward to the original
\eqn{\theta} scale, averaging over the empirical distribution of \code{s}. Per
Walters Ch 2.6, the result is a discretized pushforward summary, NOT a
newly estimated prior and NOT a closed-form Jacobian density.
}
\details{
Implements the theta-scale pushforward of Walters Ch 2.6, the inverse
direction of \code{\link[=eb_standardize]{eb_standardize()}}. Mappings:

\itemize{
\item Multiplicative: \eqn{\theta = \exp(\hat\psi_1 + \hat\psi_2 \log s) \cdot r}.
\item Additive: \eqn{\theta = \hat\psi_1 + \exp(\hat\psi_2 \log s) \cdot r} (\code{psi_1} plays the additive-intercept role).
}

The sandwich VCV from the input prior is NOT carried forward because it is
defined for the free spline coefficients on the original r scale and does
not transfer directly to the theta-scale object. If delta-method standard
errors are required, call \code{\link[=eb_delta_method]{eb_delta_method()}} on the input r-scale prior
BEFORE calling \code{eb_change_of_variables()}.

Computationally, the function (i) converts the discretized r-scale density
into grid masses, (ii) transforms those masses through the chosen mapping
across all supplied \code{s}, (iii) snaps the transformed masses onto a common
\eqn{\theta} grid via nearest-neighbor binning, and (iv) renormalizes.
This is a discretized approximation to the pushforward density; expect
smoothing artifacts at the support boundary.
}
\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = utils::head(krw_firms$theta_hat_race, 80),
  s = utils::head(krw_firms$se_race, 80)
)

\donttest{
# Heavy: requires eb_deconvolve() (~1-3 s on 80 firms with grid_size = 100).
diag_fit <- eb_diagnose(est, precision_models = "multiplicative")
std_est <- eb_standardize(est, model = "multiplicative",
                          diagnostic = diag_fit)
prior_r <- eb_deconvolve(std_est, grid_size = 100, penalty = "none")
fit <- attr(std_est, "precision_fit")

prior_theta <- eb_change_of_variables(
  prior_r,
  s = std_est$original_s,
  psi_1 = fit$psi_1,
  psi_2 = fit$psi_2,
  model = "multiplicative"
)

prior_theta$scale
head(prior_theta$support)
}

}
\seealso{
\code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_delta_method]{eb_delta_method()}}, \code{\link[=eb_standardize]{eb_standardize()}},
\code{\link[=eb_posterior_grid]{eb_posterior_grid()}}

Other eb_prior: 
\code{\link{as_deconvolveR}()},
\code{\link{from_deconvolveR}()}
}
\concept{eb_prior}
