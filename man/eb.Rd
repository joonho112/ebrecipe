% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eb.R
\name{eb}
\alias{eb}
\title{Run a complete empirical Bayes analysis}
\usage{
eb(
  x = NULL,
  s = 1,
  ...,
  formula = NULL,
  data = NULL,
  se = NULL,
  method = c("deconv", "linear", "parametric"),
  heteroskedastic = TRUE,
  output = "all",
  control = eb_control()
)
}
\arguments{
\item{x}{A numeric vector of unit-level estimates for the vector interface.}

\item{s}{A numeric vector of standard errors or a scalar recycled across
\code{x}.}

\item{...}{Additional arguments forwarded to \code{\link[=eb_input]{eb_input()}}. The current
interfaces recognize optional \code{unit_id}, \code{n}, \code{covariates}, and
\code{description}.}

\item{formula}{An optional summary-data formula such as \code{estimate ~ 1} or
\code{estimate ~ covariate}.}

\item{data}{A data frame used with \code{formula}.}

\item{se}{Standard-error input for the formula interface. Either a length-1
character naming a column in \code{data}, or a numeric vector aligned with
\code{data}.}

\item{method}{Empirical Bayes method. \code{"deconv"} runs the log-spline
nonparametric path of \code{\link[=eb_deconvolve]{eb_deconvolve()}}. \code{"linear"} and \code{"parametric"}
share the closed-form normal-prior shrinkage path in the monolith.}

\item{heteroskedastic}{Logical; whether heteroskedasticity is allowed in the
monolithic workflow.}

\item{output}{Output level. \code{"all"} returns the full fit including
classification; other values skip the classification layer.}

\item{control}{An \code{\link[=eb_control]{eb_control()}} configuration object.}
}
\value{
An \code{eb_fit} object: a list with class \code{c("eb_fit", "list")} and
the following fields:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{method}}{Character scalar reporting the method actually used (\code{"deconv"}, \code{"linear"}, or \code{"parametric"}).}
\item{\code{estimates}}{The validated input \code{eb_estimates} (always on the original scale, never standardized).}
\item{\code{prior}}{An \code{eb_prior} object holding the fitted prior.}
\item{\code{posterior}}{An \code{eb_posterior} object with the posterior table.}
\item{\code{hyperparameters}}{Named list with \code{input}, \code{analysis}, and \code{prior} blocks of method-of-moments and fitted hyperparameters.}
\item{\code{log_likelihood}}{Numeric scalar; \code{NA_real_} for the linear path when no marginal likelihood is computed.}
\item{\code{convergence}}{Named list reporting \code{converged}, \code{stage}, \code{method}, and \code{optimizer}.}
\item{\code{precision_dep}}{An \code{eb_diagnostic} summary of the precision-dependence tests.}
\item{\code{classification}}{An \code{eb_classification} object when \code{output = "all"}; otherwise \code{NULL}.}
\item{\code{control}}{The validated \code{eb_control} object.}
}
}
\description{
Fit an empirical Bayes prior, compute posterior summaries, and (optionally)
run FDR classification in a single call. \code{eb()} is the user-facing
monolith that delegates to the same six-stage pipeline a power user would
invoke by hand; both paths produce numerically identical fits per the
DEC-203 lock.
}
\details{
\code{eb()} is the umbrella entry point for Walters Ch 2 (the 6-stage
discrimination pipeline). It supports two input interfaces (vector via
\code{x}/\code{s}, or summary-data via \code{formula}/\code{data}/\code{se}) and runs:

\enumerate{
\item validate inputs (\code{\link[=eb_input]{eb_input()}});
\item precision-dependence diagnostics (Walters Ch 2.6);
\item optional standardization (Walters Ch 2.6 eq. 55);
\item prior fitting (Walters Ch 2.4 linear or Ch 5 NP);
\item posterior shrinkage (Walters Ch 5 eq. 8);
\item optional FDR-controlled classification (Walters Ch 3).
}

Standardization runs only when all three hold: \code{heteroskedastic = TRUE},
\code{control$standardize = TRUE}, and \code{control$precision_model != "none"}.
Otherwise the function skips standardization and proceeds on the supplied
estimate scale.

Per \strong{DEC-203}, the monolith and the explicit pipeline produce numerically
identical \code{eb_fit} objects on the same inputs. Choose the monolith for
one-shot reports and the pipeline for stage-level introspection or
per-stage overrides.
}
\section{Decision tree -- monolith vs. pipeline}{

\itemize{
\item Use \code{eb()} when defaults are trusted and one-shot reports suffice.
\item Use the 6-stage pipeline (\code{\link[=eb_input]{eb_input()}} -> \code{\link[=eb_diagnose]{eb_diagnose()}} -> \code{\link[=eb_standardize]{eb_standardize()}} -> \code{\link[=eb_deconvolve]{eb_deconvolve()}} -> \code{\link[=eb_shrink]{eb_shrink()}} -> \code{\link[=eb_classify]{eb_classify()}}) for introspection or per-stage overrides.
}
Both paths produce numerically identical results (DEC-203 lock).
}

\examples{
data("krw_firms", package = "ebrecipe")

krw_small <- utils::head(krw_firms, 120)

# Linear path is fast and avoids the NP optimizer.
fit <- eb(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  unit_id = krw_small$firm_id,
  method = "linear",
  control = eb_control(standardize = FALSE, precision_model = "none")
)
fit$method
fit$classification$n_selected

\donttest{
# NP deconvolution path; ~1-3 s on 120 firms with grid_size = 100.
fit_np <- eb(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  method = "deconv",
  control = eb_control(n_grid = 100, penalty = "none", standardize = FALSE)
)
fit_np$prior$method
}

}
\seealso{
\code{\link[=eb_control]{eb_control()}}, \code{\link[=eb_input]{eb_input()}}, \code{\link[=eb_test]{eb_test()}}, \code{\link[=eb_vam]{eb_vam()}},
\code{\link[=eb_deconvolve]{eb_deconvolve()}},
\code{\link[=tidy.eb_fit]{tidy.eb_fit()}}, \code{\link[=glance.eb_fit]{glance.eb_fit()}}, \code{\link[=augment.eb_fit]{augment.eb_fit()}},
\code{\link[=autoplot.eb_fit]{autoplot.eb_fit()}}

Other eb_fit: 
\code{\link{eb_control}()},
\code{\link{eb_test}()},
\code{\link{eb_vam}()}
}
\concept{eb_fit}
