% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R
\name{autoplot.eb_sim}
\alias{autoplot.eb_sim}
\alias{eb_sim_broom}
\alias{tidy.eb_sim}
\alias{glance.eb_sim}
\title{Broom and ggplot2 methods for \code{eb_sim} objects}
\usage{
autoplot.eb_sim(x, bins = 30L, ...)

tidy.eb_sim(x, ...)

glance.eb_sim(x, ...)
}
\arguments{
\item{x}{An \code{eb_sim} object.}

\item{bins}{Number of histogram bins (default \code{30}).}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.

A school-level data frame with \code{term}, \code{estimate}, and \code{n}.

A one-row data frame.
}
\description{
Visualizes the simulated school-level truth via a histogram of true
theta values; also overlays a vertical reference at the empirical
mean.

\code{tidy()} returns one row per simulated school using \code{x$schools}. The
\code{estimate} column carries the true theta (column \code{theta} if present;
falls back to \code{theta_true}). When the schools table carries \code{n_students}
(or \code{n}) it is propagated as the \code{n} column; otherwise \code{n} is \code{NA}.
Student-level draws stay on \code{x$students}; data-generating-process metadata
stays on \code{x$dgp}.

Returns a one-row data frame summarising the simulation: number of
schools, total student rows, and selected DGP scalars (\code{sigma_theta},
\code{design}) when present in \code{x$dgp}.
}
