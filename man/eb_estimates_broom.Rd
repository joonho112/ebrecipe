% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R
\name{autoplot.eb_estimates}
\alias{autoplot.eb_estimates}
\alias{eb_estimates_broom}
\alias{tidy.eb_estimates}
\alias{glance.eb_estimates}
\alias{augment.eb_estimates}
\title{Broom and ggplot2 methods for \code{eb_estimates} objects}
\usage{
autoplot.eb_estimates(x, k = 1.96, ...)

tidy.eb_estimates(x, ...)

glance.eb_estimates(x, ...)

augment.eb_estimates(x, data = NULL, ...)
}
\arguments{
\item{x}{An \code{eb_estimates} object.}

\item{k}{Width multiplier on \code{std.error} for the whisker bars
(default \code{1.96}, approximate 95\% normal interval).}

\item{...}{Unused.}

\item{data}{Optional data frame to bind columns onto (default: NULL, which
returns the unit-level table directly).}
}
\value{
A \code{ggplot} object.

A unit-level data frame with \code{term}, \code{estimate}, \code{std.error}, and
optionally \code{n}.

A one-row data frame.

A data frame with input columns plus \code{.theta_hat}, \code{.s}, optional
\code{.unit_id} and \code{.n} columns.
}
\description{
\code{autoplot.eb_estimates()} draws a forest plot of observed estimates with
\code{k * std.error} whisker bars. Units are sorted by \code{estimate} for
readability and the chart is flipped horizontally so unit labels read
left-to-right.

\code{tidy()} returns one row per unit with the observed point estimate
(\code{estimate = theta_hat}) and its standard error (\code{std.error = s}). When the
object carries a sample-size vector (\code{x$n}), it is appended as the \code{n}
column; otherwise \code{n} is omitted.

Returns a one-row data frame with \code{nobs} (number of units), the source
label (\code{source}), whether the object has been standardized
(\code{standardized}), and the empirical-Bayes hyperparameters
(\code{mu_hat}, \code{sigma_sq_hat}) when populated.

\code{augment()} returns the unit-level table with \code{.theta_hat}, \code{.s}, and any
covariate columns the object carries. When \code{data} is supplied, columns are
bound by row to the input frame (which must have one row per unit).
}
