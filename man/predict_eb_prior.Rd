% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-methods.R
\name{predict_eb_prior}
\alias{predict_eb_prior}
\alias{predict.eb_prior}
\title{Generate posterior predictions from an \code{eb_prior}}
\usage{
\method{predict}{eb_prior}(
  object,
  newdata = NULL,
  x = NULL,
  s = NULL,
  estimates = NULL,
  method = NULL,
  unstandardize = TRUE,
  formula = NULL,
  se = NULL,
  unit_id = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{eb_prior} object.}

\item{newdata}{Optional new data used to build prediction estimates.}

\item{x}{Optional estimate vector used with \code{s}.}

\item{s}{Optional standard-error vector used with \code{x}.}

\item{estimates}{Optional \code{eb_estimates} object supplied directly.}

\item{method}{Optional shrinkage method passed to \code{\link[=eb_shrink]{eb_shrink()}}.}

\item{unstandardize}{Logical flag forwarded to \code{\link[=eb_shrink]{eb_shrink()}}.}

\item{formula}{Optional monolithic formula used when \code{newdata} contains raw
columns rather than precomputed estimates.}

\item{se}{Optional standard-error specification used with \code{formula}.}

\item{unit_id}{Optional unit identifiers for vector-input predictions.}

\item{...}{Additional arguments passed to downstream prediction helpers.}
}
\value{
An \code{eb_posterior} data frame, specifically the stored posterior
table returned by \code{\link[=eb_shrink]{eb_shrink()}}.
}
\description{
\code{predict.eb_prior()} turns new estimates into posterior summaries using the
supplied prior.
}
\details{
New inputs can be supplied in three ways:
\itemize{
\item directly as an \code{eb_estimates} object via \code{estimates}
\item through \code{newdata}, either as a simple \code{theta_hat}/\code{s} table or as a
monolithic formula interface with \code{formula} and \code{se}
\item through raw vectors \code{x} and \code{s}
}

If \code{method} is left at \code{NULL}, the function auto-selects \code{"linear"} for
normal or theta-scale priors and \code{"nonparametric"} otherwise.
}
\examples{
residual_est <- eb_input(
  theta_hat = c(-0.10, 0.05, 0.20, 0.35),
  s = c(0.20, 0.20, 0.20, 0.20)
)
prior <- eb_deconvolve(
  residual_est,
  penalty = "fixed",
  penalty_value = 0.03,
  characteristic = "male"
)

pred <- predict(prior, x = c(0.00, 0.10), s = c(0.20, 0.20))
pred[, c(".theta_hat", ".posterior_mean")]

}
