% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eb-test.R
\name{eb_test}
\alias{eb_test}
\title{Run EB hypothesis testing and FDR-controlled selection}
\usage{
eb_test(
  formula = NULL,
  data = NULL,
  se = NULL,
  x = NULL,
  s = 1,
  threshold = 0,
  alternative = c("greater", "less", "two.sided"),
  fdr_level = 0.05,
  pi0_method = "storey",
  control = eb_control(),
  ...
)
}
\arguments{
\item{formula}{An optional summary-data formula for the data.frame interface.}

\item{data}{A data frame used with \code{formula}.}

\item{se}{The standard-error input for the formula interface. Supply either
a length-1 character naming a column in \code{data}, or a numeric vector
aligned with \code{data}.}

\item{x}{A numeric vector of unit-level estimates.}

\item{s}{A numeric vector of standard errors or a scalar default.}

\item{threshold}{Testing threshold \eqn{\tau} under the alternative null.
Classification is applied to \code{theta_hat - threshold}, not to the raw
estimate itself.}

\item{alternative}{Alternative hypothesis direction; one of \code{"greater"},
\code{"less"}, or \code{"two.sided"}.}

\item{fdr_level}{False discovery rate target \eqn{\alpha} used by the
classification layer.}

\item{pi0_method}{Null-proportion estimation method used inside the q-value
classification step. Use \code{"storey"} to estimate \eqn{\pi_0} from
(shifted) p-values, or \code{"fixed"} to treat \code{control$pi0_lambda} as the
fixed null proportion.}

\item{control}{An \code{\link[=eb_control]{eb_control()}} configuration object.}

\item{...}{Additional arguments forwarded to \code{\link[=eb]{eb()}}. In practice this can
include monolith arguments such as \code{method}, \code{unit_id}, \code{n},
\code{covariates}, and \code{description}.}
}
\value{
An \code{eb_test} object: a list with class
\code{c("eb_test", "eb_fit", "list")}. Slots inherited from \code{\link[=eb]{eb()}} are
preserved; testing-specific fields:
\describe{
\item{\code{classification}}{An \code{eb_classification} object built on the threshold-shifted estimates with \code{method = "qvalue"}. Never \code{NULL} (\code{eb_test()} always classifies).}
\item{\code{control$fdr_threshold}}{Numeric scalar set to \code{fdr_level}.}
\item{\code{control$pi0_method}}{Character scalar; either \code{"storey"} or \code{"fixed"} as supplied.}
\item{\code{attr(result, "test_settings")}}{Named list with \code{threshold} (numeric scalar) and \code{alternative} (character scalar). Both non-\code{NA}.}
}
}
\description{
Fit an empirical Bayes prior, then apply a q-value FDR rule to a
threshold-shifted copy of the estimates. Use this when you have a
non-zero null \eqn{\tau} and want to select units that exceed it at a
chosen FDR level \eqn{\alpha}; for plain classification on the raw
posterior or \eqn{\hat\theta} scale, use \code{\link[=eb_classify]{eb_classify()}}.
}
\details{
Implements the FDR-controlled decision rule of Walters Ch 3.4 eq. 103: fit
the prior and posterior on the original estimate scale (via \code{\link[=eb]{eb()}}), then
apply the q-value rule to \eqn{\hat\theta_j - \tau}. The
\code{fit$estimates}/\code{fit$posterior} slots remain on the original scale; only
the classification step sees \eqn{\hat\theta_j - \tau}. The testing
configuration is recorded in \code{attr(result, "test_settings")}.

This separation is deliberate. The prior summarizes the data-generating
process; the testing rule answers the question "which units exceed the
threshold \eqn{\tau} under the selected alternative?".

When \code{pi0_method = "fixed"}, \code{eb_test()} does NOT re-estimate \eqn{\pi_0}
from the shifted p-values; it forwards \code{control$pi0_lambda} to the q-value
classification step as the user-supplied null proportion.
}
\section{Decision tree -- when to test vs. classify}{

\itemize{
\item Use \code{\link[=eb_test]{eb_test()}} when the threshold \eqn{\tau} is non-zero and you want FDR-controlled selection at level \eqn{\alpha}.
\item Use \code{\link[=eb_classify]{eb_classify()}} for non-test classification rules (posterior_mean threshold, top-share).
}
}

\examples{
data("krw_firms", package = "ebrecipe")

krw_small <- utils::head(krw_firms, 120)

fit <- eb_test(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  threshold = 0.02,
  alternative = "greater",
  fdr_level = 0.10,
  method = "linear",
  control = eb_control(standardize = FALSE, precision_model = "none")
)

fit$classification$n_selected
attr(fit, "test_settings")

}
\seealso{
\code{\link[=eb]{eb()}}, \code{\link[=eb_classify]{eb_classify()}}, \code{\link[=eb_control]{eb_control()}},
\code{\link[=tidy.eb_fit]{tidy.eb_fit()}}, \code{\link[=glance.eb_fit]{glance.eb_fit()}}, \code{\link[=augment.eb_fit]{augment.eb_fit()}},
\code{\link[=autoplot.eb_fit]{autoplot.eb_fit()}}

Other eb_fit: 
\code{\link{eb}()},
\code{\link{eb_control}()},
\code{\link{eb_vam}()}
}
\concept{eb_fit}
