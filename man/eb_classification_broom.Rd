% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R, R/fortify.R
\name{autoplot.eb_classification}
\alias{autoplot.eb_classification}
\alias{eb_classification_broom}
\alias{tidy.eb_classification}
\alias{glance.eb_classification}
\alias{augment.eb_classification}
\alias{fortify.eb_classification}
\title{Broom and ggplot2 methods for \code{eb_classification} objects}
\usage{
autoplot.eb_classification(x, bins = 30L, ...)

tidy.eb_classification(x, ...)

glance.eb_classification(x, ...)

augment.eb_classification(x, data = NULL, ...)

fortify.eb_classification(model, data, ...)
}
\arguments{
\item{x}{An \code{eb_classification} object.}

\item{bins}{Number of histogram bins (default \code{30}).}

\item{...}{Forwarded to \code{tidy.eb_classification()}.}

\item{data}{Unused (kept for ggplot2 fortify generic signature).}

\item{model}{An \code{eb_classification} object.}
}
\value{
A \code{ggplot} object.

A unit-level data frame with \code{term}, \code{p.value}, \code{q.value}, and
\code{selected}.

A one-row data frame.

A data frame with augmented classification columns.

A \code{data.frame} (the result of \code{tidy.eb_classification(model, ...)}).
}
\description{
Visualizes the classification result as a histogram of p-values overlaid
with the proportion \code{pi0} of estimated null units (horizontal reference)
and a vertical line at the empirical p-value cutoff implied by the
selected set.

\code{tidy()} returns the unit-level classification table with raw p-values,
q-values, and the selected-set indicator. Scalar metadata such as \code{pi0} and
any frontier summary remain on the original object.

Returns a one-row data frame with \code{nobs}, \code{n_selected}, \code{pi0},
\code{pi0_method}, and the threshold/method used to classify.

\code{augment()} returns the per-unit classification table joined with the
input \code{data} (when supplied). Columns added: \code{.p_value}, \code{.q_value},
\code{.selected}.

Equivalent to \code{as.data.frame(tidy(model))}.
}
\examples{
data("krw_firms", package = "ebrecipe")
krw_small <- utils::head(krw_firms, 80)

fit <- eb(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)
post <- eb_shrink(fit$estimates, fit$prior, method = "linear")
cls <- eb_classify(
  estimates = fit$estimates,
  posterior = post,
  method = "qvalue",
  frontier = FALSE
)

if (requireNamespace("broom", quietly = TRUE)) {
  broom::tidy(cls)
}

}
