% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-cli-helpers.R
\name{format_eb_cli}
\alias{format_eb_cli}
\alias{format_eb_estimates_cli}
\alias{format_eb_prior_cli}
\alias{format_eb_posterior_cli}
\alias{format_eb_diagnostic_cli}
\alias{format_eb_classification_cli}
\alias{format_eb_fit_cli}
\alias{format_eb_vam_fit_cli}
\alias{format_eb_precision_fit_cli}
\title{Render an \verb{eb_*} object with cli decoration}
\usage{
format_eb_estimates_cli(x, ...)

format_eb_prior_cli(x, ...)

format_eb_posterior_cli(x, ...)

format_eb_diagnostic_cli(x, ...)

format_eb_classification_cli(x, ...)

format_eb_fit_cli(x, ...)

format_eb_vam_fit_cli(x, ...)

format_eb_precision_fit_cli(x, ...)
}
\arguments{
\item{x}{An object of the matching \verb{eb_*} class. The helper does not
class-check explicitly; the underlying \verb{format_eb_<class>()} reports
informatively if \code{x} is the wrong type.}

\item{...}{Forwarded to \verb{format_eb_<class>(x, ...)}. Currently unused by
any of the eight base formatters but reserved for forward-compatible
options (e.g. \verb{digits =}).}
}
\value{
Invisibly returns \code{x} so the helpers are chainable in a pipeline
(\code{x |> format_eb_estimates_cli() |> some_next_step()}). The cli output
is a side effect on the active connection.
}
\description{
Opt-in companions to the \code{print()} and \code{summary()} methods for the eight
\verb{eb_*} classes. Each helper takes an object of the matching class, runs it
through the corresponding internal \verb{format_eb_<class>()} base formatter to
obtain the canonical \code{character()} body, then forwards that body to the
internal \verb{.cli_eb_<class>()} decorator (cli h1 banner + cli_text body
lines).
}
\details{
These helpers exist as a deliberate consequence of the documented design
decision. The v1 invariant
\code{identical(x, eval(parse(text = capture.output(print(x)))))} -- relied on
by stream-capturing utilities, snapshot tests, and reproducible logs --
broke when v2 \code{print()} / \code{summary()} bodies emitted cli decorations
directly. The design decision mandated that the method bodies remain plain
\code{cat() + invisible()} and that all cli decoration be factored into these
eight opt-in \verb{format_eb_*_cli()} wrappers. The user-facing decorated
display therefore becomes an explicit call, not an implicit side effect of
printing.

Per redesign decision DEC-124-1 ("zero hard CRAN deps"), the \code{cli} package
lives in \code{Suggests}. Calling any of these helpers without \code{cli} installed
errors with an install hint. For a guaranteed-render path that works
without \code{cli}, use \code{print()} on the object (or \verb{format_eb_<class>()} for
the raw \code{character()} vector).
}
\section{Why invisible(x)}{

Three return-shape options were considered: (a) \code{invisible(x)},
(b) \code{invisible(NULL)}, (c) the \code{character()} vector from
\verb{format_eb_<class>()}. We chose (a): chainability matches the tidyverse
\code{print()} convention, lets these helpers slot into a \verb{|>} chain without
breaking flow, and keeps the cli output as the visible-on-screen artefact
while the value passes through.
}

\examples{
if (interactive() && requireNamespace("cli", quietly = TRUE)) {
  data("krw_firms", package = "ebrecipe")
  est <- eb_input(
    theta_hat = utils::head(krw_firms$theta_hat_race, 20),
    s         = utils::head(krw_firms$se_race,        20)
  )
  format_eb_estimates_cli(est)
}

}
\seealso{
\code{format_eb_estimates()}, \code{format_eb_prior()}, \code{format_eb_posterior()},
\code{format_eb_diagnostic()}, \code{format_eb_classification()}, \code{format_eb_fit()},
\code{format_eb_vam_fit()}, \code{format_eb_precision_fit()} for the underlying
character-vector formatters; \code{\link[=print.eb_estimates]{print.eb_estimates()}} (and the other class
\verb{print.*} methods) for the default base-layer rendering that preserves
the \code{capture.output()} invariant.
}
\concept{eb_cli}
