% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-deconvolveR.R
\name{as_deconvolveR}
\alias{as_deconvolveR}
\title{Coerce an eb_prior to a deconvolveR-compatible result list}
\usage{
as_deconvolveR(prior, ...)
}
\arguments{
\item{prior}{An \code{eb_prior} object (any backend), typically from
\code{\link[=eb_deconvolve]{eb_deconvolve()}} or \code{\link[=from_deconvolveR]{from_deconvolveR()}}.}

\item{...}{Reserved for future arguments. Ignored at present.}
}
\value{
A \code{c("deconvolveR_result", "list")} list mirroring
\code{deconvolveR::deconv()}'s output shape:
\describe{
\item{\code{mle}}{Numeric vector -- the prior's \code{alpha} (spline coefficients). Never \code{NA} for native logspline priors.}
\item{\code{Q}}{Numeric matrix or \code{NULL} -- deconvolveR's Q matrix. \code{NULL} unless the prior was originally produced via \code{\link[=from_deconvolveR]{from_deconvolveR()}} and cached \code{Q} in \code{spline_info}.}
\item{\code{P}}{Numeric matrix or \code{NULL} -- deconvolveR's P matrix. Same caching caveat as \code{Q}.}
\item{\code{S}}{Numeric scalar or \code{NA_real_} -- deconvolveR's penalty scalar. \code{NA} when not cached.}
\item{\code{cov}}{Numeric matrix or \code{NULL} -- the prior's \code{V} (parameter covariance). \code{NULL} for priors with no covariance estimate.}
\item{\code{cov.g}}{Numeric matrix or \code{NULL} -- deconvolveR's G covariance. \code{NULL} when not cached.}
\item{\code{stats}}{Numeric matrix with columns \code{theta}, \code{g}, \code{SE.g}, \code{G}, \code{SE.G}, \code{Bias.g}. \code{theta}/\code{g}/\code{G} are always populated; \code{SE.g}/\code{SE.G}/\code{Bias.g} are \code{NA_real_} when not cached on the source prior.}
\item{\code{loglik}}{Numeric scalar or \code{NULL} -- deconvolveR's log-likelihood. \code{NULL} when not cached.}
\item{\code{statsFunction}}{Function or \code{NULL} -- deconvolveR's stats closure. \code{NULL} when not cached.}
}
}
\description{
Bridges an \code{ebrecipe} \code{eb_prior} into the list shape returned by
\code{deconvolveR::deconv()} so that native and deconvolveR-backend priors can
be compared side by side. This is a comparison bridge, NOT a promise of
full fidelity -- many \code{deconvolveR} quantities are reconstructed
best-effort or returned as \code{NULL}/\code{NA} if not cached.
}
\details{
The returned object is intended for inspection, side-by-side comparisons,
and lightweight round-tripping in tests. It should not be interpreted as a
complete recreation of every quantity produced by \code{deconvolveR::deconv()}
(Walters Ch 5.4 discusses the discrete-spline backend choice).

The bridge preserves the prior's current support scale -- it does not
standardize, re-fit, or infer a common-\eqn{\sigma} scale on its own. When
\verb{prior$spline_info$deconvolveR_*} fields are present (i.e. when \code{prior}
originally came from \code{\link[=from_deconvolveR]{from_deconvolveR()}}), those cached quantities are
reused verbatim; otherwise \code{Q}, \code{P}, \code{cov.g}, \code{loglik}, \code{statsFunction},
and the auxiliary uncertainty columns in \code{stats} (\code{SE.g}, \code{SE.G},
\code{Bias.g}) are filled with \code{NULL} or \code{NA_real_}. Mass renormalization is
performed via an internal helper so that the returned \code{stats[, "g"]} sums
to 1 and \code{stats[, "G"]} is its cumulative sum.
}
\section{Decision tree -- when to use which prior bridge}{

\itemize{
\item Use \code{\link[=as_deconvolveR]{as_deconvolveR()}} to coerce an \code{ebrecipe} prior INTO a deconvolveR-shaped list.
\item Use \code{\link[=from_deconvolveR]{from_deconvolveR()}} to wrap a \code{deconvolveR::deconv()} result AS an \code{ebrecipe} prior.
\item Use \code{\link[=eb_deconvolve]{eb_deconvolve()}} with \code{method = "deconvolver"} for end-to-end fitting through deconvolveR.
}
}

\examples{
# Round-trip: deconvolveR-shaped raw -> eb_prior -> deconvolveR-shaped list.
raw <- list(
  mle = c(0.1, -0.2),
  stats = cbind(
    theta = c(-1, 0, 1),
    g     = c(0.2, 0.6, 0.2)
  )
)
prior  <- from_deconvolveR(raw, sigma = 1, scale = "theta")
bridge <- as_deconvolveR(prior)
bridge$stats[, c("theta", "g", "G")]
class(bridge)

}
\seealso{
\code{\link[=from_deconvolveR]{from_deconvolveR()}}, \code{\link[=eb_deconvolve]{eb_deconvolve()}},
\code{\link[=tidy.eb_prior]{tidy.eb_prior()}}, \code{\link[=glance.eb_prior]{glance.eb_prior()}}, \code{\link[=autoplot.eb_prior]{autoplot.eb_prior()}}

Other eb_prior: 
\code{\link{eb_change_of_variables}()},
\code{\link{from_deconvolveR}()}
}
\concept{eb_prior}
