% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-conditional.R
\name{eb_shrink_conditional}
\alias{eb_shrink_conditional}
\title{Compute conditional linear empirical Bayes shrinkage}
\usage{
eb_shrink_conditional(
  estimates,
  formula,
  sigma_sq = NULL,
  control = eb_control(),
  ...
)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object. Must carry a \code{covariates} data
frame containing every variable named in \code{formula}.}

\item{formula}{One-sided formula defining conditioning covariates (e.g.
\code{~ charter}). Variables are looked up in \code{estimates$covariates}.}

\item{sigma_sq}{Optional residual signal variance override
\eqn{\sigma_r^2}. When omitted, uses the conditional variance estimate
implied by \code{formula}.}

\item{control}{Control settings from \code{\link[=eb_control]{eb_control()}}. Currently validated
for API consistency but otherwise unused on this path.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
An \code{eb_posterior} object whose \code{posterior} data frame has nine
columns:
\describe{
\item{\code{.unit_id}}{Unit identifier (or \code{seq_along()} if absent).}
\item{\code{.theta_hat}}{Observed estimate on the theta scale.}
\item{\code{.s}}{Standard error on the theta scale.}
\item{\code{.prior_mean}}{Conditional prior mean \eqn{Z_j' \hat\mu}.}
\item{\code{.posterior_mean}}{Posterior mean \eqn{w_j \hat\theta_j + (1 - w_j) Z_j' \hat\mu}.}
\item{\code{.posterior_sd}}{Currently placeholder \code{NA_real_}.}
\item{\code{.shrinkage_weight}}{Linear weight \eqn{w_j = \sigma_r^2 / (\sigma_r^2 + s_j^2)} in \eqn{[0, 1]}.}
\item{\code{.ci_lower}}{Currently placeholder \code{NA_real_}.}
\item{\code{.ci_upper}}{Currently placeholder \code{NA_real_}.}
}
\code{method} on the returned object is always \code{"conditional_linear"}.
\code{prior} is an \code{eb_prior} with \code{method = "normal"} carrying the
conditional hyperparameters.
}
\description{
Linear EB shrinkage with a covariate-dependent prior mean. Each unit is
shrunk toward its conditional mean (e.g. sector mean for charter vs.
traditional schools) instead of a single global mean. Per Walters Ch 4.3,
the residual signal variance is treated as common across the conditioning
groups.
}
\details{
Implements the conditional linear EB path of Walters Ch 4.3. This is NOT
a nonparametric routine; the public contract is a conditional normal-prior
approximation:

\itemize{
\item Unit-specific prior mean \eqn{Z_j' \hat\mu} from the conditioning model.
\item Common residual signal variance \eqn{\sigma_r^2}.
\item Linear shrinkage weight \eqn{w_j = \sigma_r^2 / (\sigma_r^2 + s_j^2)}, with closed-form posterior \eqn{\tilde\theta_j = w_j \hat\theta_j + (1 - w_j) Z_j' \hat\mu}.
}

Always operates on the observed \eqn{\theta} scale. Unlike \code{\link[=eb_shrink]{eb_shrink()}}
there is NO standardize/unstandardize stage. A defensive bound enforces
\eqn{w_j \in \eqn{[0, 1]}} and errors on violation (tripwire for upstream
regressions in the conditional hyperparameter fit).
}
\examples{
est <- eb_input(
  theta_hat = c(-0.20, -0.05, 0.10, 0.25),
  s = c(0.10, 0.12, 0.09, 0.11),
  covariates = data.frame(charter = c(FALSE, FALSE, TRUE, TRUE))
)

post <- eb_shrink_conditional(est, ~ charter)

post$posterior[, c(".prior_mean", ".posterior_mean", ".shrinkage_weight")]

}
\seealso{
\code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_vam]{eb_vam()}}, \code{\link[=eb_reliability]{eb_reliability()}}

Other eb_posterior: 
\code{\link{eb_mse}()},
\code{\link{eb_posterior_grid}()},
\code{\link{eb_reliability}()},
\code{\link{eb_shrink}()}
}
\concept{eb_posterior}
