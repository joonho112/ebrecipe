% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R
\name{autoplot.eb_diagnostic}
\alias{autoplot.eb_diagnostic}
\alias{eb_diagnostic_broom}
\alias{tidy.eb_diagnostic}
\alias{glance.eb_diagnostic}
\title{Broom and ggplot2 methods for \code{eb_diagnostic} objects}
\usage{
autoplot.eb_diagnostic(x, ...)

tidy.eb_diagnostic(x, ...)

glance.eb_diagnostic(x, ...)
}
\arguments{
\item{x}{An \code{eb_diagnostic} object.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.

\code{tidy()} returns a long data frame with \code{component}, \code{term},
\code{estimate}, \code{std.error}, \code{statistic}, and \code{p.value}. \code{glance()} returns a
one-row diagnostic summary.
}
\description{
Visualizes the precision-dependence diagnostic. Stacks coefficient
rows from \code{tidy.eb_diagnostic()} (level test, variance test,
multiplicative/additive parameter rows) as a \code{geom_pointrange()} chart
with \verb{+/- 1.96 * std.error} whiskers, faceted by the diagnostic component.

\code{tidy()} stacks regression-style rows for the level test, variance test, and
any fitted precision-dependence models. \code{glance()} returns a one-row summary
of the overall diagnostic conclusion.
}
\examples{
data("krw_firms", package = "ebrecipe")
krw_small <- utils::head(krw_firms, 80)

diag_fit <- eb_diagnose(
  eb_input(
    theta_hat = krw_small$theta_hat_race,
    s = krw_small$se_race
  )
)

if (requireNamespace("broom", quietly = TRUE)) {
  broom::tidy(diag_fit)
  broom::glance(diag_fit)
}

}
