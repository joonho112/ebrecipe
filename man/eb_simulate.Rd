% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{eb_simulate}
\alias{eb_simulate}
\title{Simulate value-added data with known ground truth}
\usage{
eb_simulate(
  n_units = 50,
  n_obs = 2500,
  sigma_theta = 0.2,
  design = c("balanced", "unbalanced"),
  groups = NULL,
  seed = NULL,
  J = NULL,
  N = NULL
)
}
\arguments{
\item{n_units}{Positive integer; number of units (schools) \eqn{J} to
simulate. Default \code{50}.}

\item{n_obs}{Positive integer; total number of observations (students)
\eqn{N} to simulate. Default \code{2500}.}

\item{sigma_theta}{Non-negative numeric scalar; standard deviation of the
latent unit signal \eqn{\theta_j \sim N(0, \sigma_\theta^2)}. Default
\code{0.20}.}

\item{design}{Character scalar; \code{"balanced"} allocates roughly equal
numbers of students per unit. \code{"unbalanced"} uses a discrete-choice
assignment rule driven by school-specific utility shocks.}

\item{groups}{Optional named list. The current public schema supports a
\code{charter} sub-list with numeric \code{share} (in \eqn{[0, 1]}) and numeric
\code{boost} (additive mean shift to \eqn{\theta_j} for charter units).}

\item{seed}{Optional integer random seed. When supplied, the function
saves and restores \code{.GlobalEnv$.Random.seed} on exit so the caller's
RNG stream is not disturbed.}

\item{J}{Optional alias for \code{n_units}, kept for workshop-style notation
(Walters writes \eqn{J} for the number of units). Overrides \code{n_units}
when supplied.}

\item{N}{Optional alias for \code{n_obs}, kept for workshop-style notation
(Walters writes \eqn{N} for the number of observations). Overrides
\code{n_obs} when supplied.}
}
\value{
An \code{eb_sim} object (S3 list) with three components:
\describe{
\item{\code{students}}{Data frame -- observation-level records (\code{student_id}, \code{school_id}, \code{x} covariate, \code{theta_true}, \code{y} outcome, \code{charter}, \code{group}); always \eqn{N} rows.}
\item{\code{schools}}{Data frame -- unit-level latent truth and assignment components (\code{school_id}, \code{theta}, \code{theta_true}, \code{delta}, \code{gamma}, \code{charter}, \code{group}, \code{n_students}); always \eqn{J} rows.}
\item{\code{dgp}}{Named list -- compact record of the simulation settings used to generate the draw (\code{n_units}, \code{n_obs}, \code{sigma_theta}, \code{design}, \code{groups}, \code{seed}, plus charter-related counts). Sufficient to reproduce the draw exactly.}
}
}
\description{
Generates a synthetic value-added (VAM) panel with known unit effects
\eqn{\theta_j} and student-level outcomes \eqn{y_{ij}}, returning a
three-table \code{eb_sim} object (\code{students}, \code{schools}, \code{dgp}). Designed for
tutorials, regression tests, and end-to-end EB workflow verification where
having access to the truth is essential.
}
\details{
The data-generating process draws unit effects
\deqn{\theta_j \sim N(0, \sigma_\theta^2),}
a student-level covariate \eqn{x_i \sim N(0,1)}, and outcomes
\deqn{y_{ij} = \theta_{j(i)} + x_i + \varepsilon_i, \quad \varepsilon_i \sim N(0,1).}
This matches the canonical homoskedastic VAM setup of Walters Ch 2.2
(eq. 5-6).

In the unbalanced design, school assignment is generated from
school-specific utility components \eqn{\delta_j}, \eqn{\gamma_j}, and
Gumbel shocks, which produces realistic unequal school sizes. The optional
\code{groups$charter} block adds an additive mean shift \code{boost} to
\eqn{\theta_j} for a \code{share} fraction of units, enabling group-conditional
shrinkage demos (Walters Ch 6.2).

Although \code{eb_simulate()} returns an \code{eb_sim} object rather than an
\code{eb_estimates} object directly, its \code{students} table is the canonical input
to \code{\link[=eb_estimate_fe]{eb_estimate_fe()}} for end-to-end shrinkage workflows; for that reason
it is grouped with the other estimate-layer constructors in the
\code{eb_estimates} family.
}
\examples{
# Reproducible 8-school, 80-student draw for a quick demo.
sim <- eb_simulate(n_units = 8, n_obs = 80, seed = 1)
nrow(sim$students)
head(sim$schools$theta_true)
sim$dgp$design

# Round-trip: simulate -> estimate FE.
est <- eb_estimate_fe(y ~ x | school_id, data = sim$students)
head(est$theta_hat)

}
\seealso{
\code{\link[=eb_input]{eb_input()}}, \code{\link[=eb_estimate_fe]{eb_estimate_fe()}}, \code{\link[=eb_estimate_groups]{eb_estimate_groups()}},
\code{\link[=eb_vam]{eb_vam()}}, \code{\link[=eb]{eb()}}, \link{vam_simulated}, \link{vam_schools}

Other eb_estimates: 
\code{\link{eb_estimate_fe}()},
\code{\link{eb_estimate_groups}()},
\code{\link{eb_input}()},
\code{\link{eb_standardize}()}
}
\concept{eb_estimates}
