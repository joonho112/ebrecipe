% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fdr-histogram.R
\name{plot_fdr_histogram}
\alias{plot_fdr_histogram}
\title{Plot companion-style p-value and q-value histograms}
\usage{
plot_fdr_histogram(
  posterior = NULL,
  classification = NULL,
  metric = c("p", "q"),
  lambda = 0.5,
  fdr_level = 0.05,
  characteristic,
  binwidth = NULL,
  annotate = TRUE,
  target_id = NULL,
  source_receipt = NULL,
  validation_mode = c("strict", "exploratory", "none")
)
}
\arguments{
\item{posterior}{Optional posterior data frame or \code{eb_posterior}/\code{eb_fit}
object. Companion \code{posteriors_white.csv} imports are accepted.}

\item{classification}{Optional \code{eb_classification}-like object containing
\code{p_values}, \code{q_values}, and \code{pi0}.}

\item{metric}{Histogram metric. \code{"p"} draws the p-value density histogram;
\code{"q"} draws the q-value frequency histogram.}

\item{lambda}{Storey threshold used when \code{posterior} is supplied without a
precomputed classification. Default \code{0.50}.}

\item{fdr_level}{FDR threshold used for selected-count metadata. Default
\code{0.05}.}

\item{characteristic}{Length-one label for the empirical characteristic,
such as \code{"white"}.}

\item{binwidth}{Histogram bin width. When \code{NULL}, companion defaults are
used: \code{0.05} for p-values and \code{0.02} for q-values.}

\item{annotate}{Logical. When \code{TRUE}, the p-value panel includes the
companion Storey-threshold and \eqn{\hat\pi_0} annotations. Ignored for
q-value panels.}

\item{target_id}{Optional internal replication target identifier.}

\item{source_receipt}{Optional companion parity source receipt. In strict
mode, protected companion targets such as \code{pval_histogram} must provide
the matching receipt so row counts and Storey q-value conventions are
checked.}

\item{validation_mode}{Target validation mode. The default \code{"strict"}
requires receipts for protected companion targets, \code{"exploratory"} checks
target metadata when possible without requiring a receipt, and \code{"none"}
disables target validation.}
}
\value{
A \code{ggplot} object. The internal \code{eb_figure_data} object used to
build the plot is stored in \code{attr(plot, "eb_figure_data")}; the companion
Figure 04-03 render contract is stored in \code{attr(plot, "eb_render_spec")}.
}
\description{
Draws the Walters (2024) companion Figure 04-03 histograms for empirical
Bayes FDR analysis. The p-value panel shows the one-tailed test p-value
distribution with the Storey threshold and \eqn{\hat\pi_0} reference line;
the q-value panel shows raw Storey-ratio q-values in firm counts.
}
\details{
The helper uses the same data contract as \code{.eb_figdata_fdr()}: when
\code{classification} is supplied, its p-values, q-values, \eqn{\pi_0}, FDR
level, and unit IDs are used directly. Otherwise the Walters upper-tail
p-values are computed from \code{posterior} columns \code{theta_hat} and \code{s}, with
q-values formed from the raw Storey ratio.

Exact Lane A companion examples require both \code{target_id} and
\code{source_receipt} so the helper can validate source assets, row counts, and
q-value conventions. Live workflow histograms should omit protected target
IDs. See \code{vignette("visualization", package = "ebrecipe")} for
receipt-backed examples.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data("krw_firms", package = "ebrecipe")
  posterior <- data.frame(
    theta_hat = krw_firms$theta_hat_race,
    s = krw_firms$se_race,
    theta_star = krw_firms$theta_hat_race,
    firm_id = krw_firms$firm_id
  )
  plot_fdr_histogram(posterior = posterior, metric = "p", characteristic = "white")
  plot_fdr_histogram(posterior = posterior, metric = "q", characteristic = "white")
}
}
}
