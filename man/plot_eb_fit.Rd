% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{plot_eb_fit}
\alias{plot_eb_fit}
\alias{plot.eb_fit}
\title{Base plotting for \code{eb_fit} objects}
\usage{
\method{plot}{eb_fit}(
  x,
  y = NULL,
  type = c("diagnostic", "prior", "shrinkage", "reliability", "posterior", "pvalue",
    "qvalue", "frontier", "volcano", "variance_ordering", "mse"),
  which = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{eb_fit} object.}

\item{y}{Unused.}

\item{type}{Plot type to construct.}

\item{which}{Optional subset of posterior rows used by \code{type = "posterior"}.}

\item{...}{Additional graphical arguments passed to the underlying base plot.}
}
\value{
The input object, invisibly.
}
\description{
\code{plot.eb_fit()} is the main base-graphics plotting entry point for fitted EB
workflows.
}
\details{
Recommended uses:
\itemize{
\item \code{"diagnostic"}: four-panel overview combining prior, shrinkage,
reliability, and residual plots
\item \code{"prior"}: prior with observed estimates
\item \code{"shrinkage"}: observed estimates versus posterior means, optionally
colored by q-values when classification output is available
\item \code{"reliability"}: shrinkage weights against standard errors
\item \code{"posterior"}: per-unit posterior summaries
\item \code{"variance_ordering"} or \code{"mse"}: higher-level comparison diagnostics
}

The plot types \code{"pvalue"}, \code{"qvalue"}, \code{"frontier"}, and \code{"volcano"} require
a fit that carries \code{classification} output.

A useful chooser is:
\itemize{
\item prior fit or observed-vs-prior comparison: \code{"prior"}
\item shrinkage behavior: \code{"shrinkage"} or \code{"reliability"}
\item posterior uncertainty for selected units: \code{"posterior"}
\item decision / FDR views: \code{"pvalue"}, \code{"qvalue"}, \code{"frontier"}, \code{"volcano"}
\item higher-level variance or risk summaries: \code{"variance_ordering"} or \code{"mse"}
}
}
\examples{
data("krw_firms", package = "ebrecipe")
krw_small <- utils::head(krw_firms, 80)

fit <- eb(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)

plot(fit, type = "shrinkage")

}
