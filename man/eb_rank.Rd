% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{eb_rank}
\alias{eb_rank}
\title{Rank units by posterior summaries}
\usage{
eb_rank(
  posterior,
  method = c("posterior_mean", "qvalue", "estimate", "posterior_probability"),
  target = NULL,
  n_sim = 1000,
  seed = NULL,
  ...
)
}
\arguments{
\item{posterior}{An \code{eb_posterior} object (e.g. from \code{\link[=eb_shrink]{eb_shrink()}} or
\code{\link[=eb]{eb()}} with \code{output = "posterior"}).}

\item{method}{Ranking criterion. One of \code{"posterior_mean"} (default),
\code{"qvalue"}, \code{"estimate"}, or \code{"posterior_probability"} (reserved).}

\item{target}{Optional ranking target \eqn{\tau} (reserved for the
\code{"posterior_probability"} rule; currently unused).}

\item{n_sim}{Number of simulations for stochastic ranking methods.
Currently unused; reserved for \code{"posterior_probability"}. Default \code{1000}.}

\item{seed}{Optional random seed for stochastic methods. Currently unused.}

\item{...}{Additional arguments forwarded to \code{\link[=eb_classify]{eb_classify()}} when
\code{method = "qvalue"} (e.g. \code{direction}, \code{pi0_method}, \code{threshold_b},
\code{fdr_level}); the controlling \code{estimates}, \code{posterior}, \code{method}, and
\code{frontier} arguments are reserved by \code{eb_rank()} and silently dropped.}
}
\value{
A data frame with one row per unit and the following columns:
\describe{
\item{\code{.unit_id}}{Unit identifier from \code{posterior$posterior$.unit_id}, or \code{posterior$estimates$unit_id}, or a \code{seq_len(J)} fallback. Type matches the source.}
\item{\code{.score}}{Numeric score used for the current ranking (posterior mean, raw estimate, or q-value).}
\item{\code{.rank}}{Numeric midrank under the current \code{method} (average tie-handling, so values may be non-integer when ties are present).}
\item{\code{.rank_original}}{Numeric midrank under the raw estimate \eqn{\hat\theta_j}, largest first.}
\item{\code{.rank_change}}{Numeric \code{.rank_original - .rank}; positive means the active rule promoted the unit relative to the raw ordering.}
\item{\code{.method}}{Character: the \code{method} argument used, repeated J times.}
}
Length is \eqn{J} = number of posterior rows; no NA rows are introduced.
}
\description{
Converts an \code{eb_posterior} object into a long-format ranking table that
attaches a score, a midrank, and the change from the original estimate
rank to each unit. Supports posterior-mean ranking (the EB shrunk
default), raw-estimate ranking (no shrinkage; useful as a benchmark),
and q-value ranking that reuses the \code{\link[=eb_classify]{eb_classify()}} FDR contract.
}
\details{
Walters Ch 3.5 (posterior-mean rank) and Ch 3.4 eq. 103 (q-value rank)
anchor the criteria. Ties are broken with the average midrank
(\code{base::rank(..., ties.method = "average")}), so reported ranks can be
non-integer when several units share the same score. For
\code{method = "qvalue"}, the function calls \code{\link[=eb_classify]{eb_classify()}} internally with
\code{frontier = FALSE} and forwards extra \code{...} arguments (such as
\code{pi0_method}, \code{threshold_b}, \code{direction}).

The \code{"posterior_probability"} criterion (rank by
\eqn{P(\theta_j > \tau \mid \mathrm{data})} for some \code{target} \eqn{\tau})
is reserved for a future enhancement; it currently raises a typed error.
}
\section{Decision tree -- which ranking rule}{

\itemize{
\item \code{method = "posterior_mean"} -- default; ranks by \eqn{E[\theta_j \mid \mathrm{data}]}.
\item \code{method = "estimate"} -- ranks by raw \eqn{\hat\theta_j} (no shrinkage).
\item \code{method = "qvalue"} -- ranks by FDR-controlled q-values.
\item \code{method = "posterior_probability"} -- ranks by \eqn{P(\theta_j > \tau \mid \mathrm{data})}; requires \verb{target = \\tau}. Reserved; raises an error in the current implementation.
}
}

\examples{
data("krw_firms", package = "ebrecipe")

fit <- eb(
  x = krw_firms$theta_hat_race,
  s = krw_firms$se_race,
  unit_id = krw_firms$firm_id,
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)
post <- eb_shrink(fit$estimates, fit$prior, method = "linear")

# Rank by posterior mean (default).
rk_pm <- eb_rank(post, method = "posterior_mean")
head(rk_pm)

# Rank by q-value (delegates to eb_classify internally).
rk_q <- eb_rank(post, method = "qvalue", direction = "upper")
head(rk_q[order(rk_q$.rank), ])

}
\seealso{
\code{\link[=eb_classify]{eb_classify()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_pi0]{eb_pi0()}}, \code{\link[=eb]{eb()}},
\code{\link[=tidy.eb_classification]{tidy.eb_classification()}}, \code{\link[=autoplot.eb_classification]{autoplot.eb_classification()}}

Other eb_classification: 
\code{\link{eb_classify}()},
\code{\link{eb_pi0}()},
\code{\link{selected_units}()}
}
\concept{eb_classification}
