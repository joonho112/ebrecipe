% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input.R
\name{eb_input}
\alias{eb_input}
\title{Wrap precomputed estimates and standard errors as \code{eb_estimates}}
\usage{
eb_input(
  theta_hat,
  s,
  unit_id = NULL,
  n = NULL,
  covariates = NULL,
  description = NULL
)
}
\arguments{
\item{theta_hat}{Numeric vector of unit-level point estimates
\eqn{\hat\theta_j}. Typically fixed effects, group means, or other unit
summaries computed outside \code{ebrecipe}. Must be finite (no \code{NA}/\code{Inf}).}

\item{s}{Numeric vector of unit-level analytical standard errors
\eqn{s_j} aligned 1-to-1 with \code{theta_hat}. Must be finite and strictly
positive; same length as \code{theta_hat}.}

\item{unit_id}{Optional vector of unit identifiers, length matching
\code{theta_hat}. Character or integer; preserved through downstream stages.
If \code{NULL}, the resulting object is still valid but carries no labels.}

\item{n}{Optional integer vector of per-unit sample sizes, length matching
\code{theta_hat}. \code{NULL} means unknown.}

\item{covariates}{Optional unit-level covariate data frame, one row per
unit, used only for downstream conditional-shrinkage or reporting hooks.
Not consumed by \code{eb_input()} itself.}

\item{description}{Optional length-1 character label describing the source
of the estimates (recorded in object metadata).}
}
\value{
An \code{eb_estimates} object (S3 list) with the following public fields:
\describe{
\item{\code{theta_hat}}{Numeric vector -- validated point estimates \eqn{\hat\theta_j}. Never \code{NA}.}
\item{\code{s}}{Numeric vector -- validated standard errors \eqn{s_j}, strictly positive. Never \code{NA}.}
\item{\code{unit_id}}{Character/integer vector or \code{NULL} -- unit labels passed through.}
\item{\code{n}}{Integer vector or \code{NULL} -- per-unit sample sizes.}
\item{\code{covariates}}{Data frame or \code{NULL} -- pass-through unit-level covariates.}
\item{\code{source}}{Character scalar -- always \code{"manual"} for \code{eb_input()}, distinguishing this entry point from \code{"unit_fe"} / \code{"group_slope"} / \code{"simulation"}.}
\item{\code{description}}{Character scalar or \code{NULL} -- user-supplied source label.}
}
}
\description{
Validates user-supplied unit-level point estimates \eqn{\hat\theta_j} and
analytical standard errors \eqn{s_j} and packages them in the standardized
\code{eb_estimates} container that all downstream \code{ebrecipe} stages consume.
Use this when your point estimates already exist (Stata, fixest, lme4, a
published table) and you only need to plug them into the EB pipeline.
}
\details{
\code{eb_input()} is purely a validate-and-wrap step -- it estimates nothing.
It enforces the EB input contract \eqn{\hat\theta_j \sim N(\theta_j, s_j^2)}
(Walters Ch 2.1 eq. 8): one independent normal likelihood per unit, with
\eqn{s_j} treated as known. Downstream stages (\code{\link[=eb_deconvolve]{eb_deconvolve()}},
\code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_vam]{eb_vam()}}) assume this contract holds.

If your \eqn{s_j} are themselves uncertain (e.g., very small per-unit sample
sizes), the input contract is still nominally satisfied but the resulting
posterior summaries inherit that noise; consider \code{\link[=eb_estimate_fe]{eb_estimate_fe()}} so that
\code{ebrecipe} controls the SE computation, or use \code{\link[=eb_simulate]{eb_simulate()}} to diagnose
the impact in a controlled DGP.
}
\section{Decision tree -- when to use which input wrapper}{

\itemize{
\item Already have a numeric vector of \eqn{\hat\theta_j} and \eqn{s_j} -> use \code{\link[=eb_input]{eb_input()}}.
\item Have student-level data and need \eqn{\hat\theta_j} from a fixed-effect regression -> use \code{\link[=eb_estimate_fe]{eb_estimate_fe()}}.
\item Have a panel of micro-level data with one treatment contrast per group -> use \code{\link[=eb_estimate_groups]{eb_estimate_groups()}}.
\item Need synthetic VAM data with known truth for testing -> use \code{\link[=eb_simulate]{eb_simulate()}}.
}
}

\examples{
data("krw_firms", package = "ebrecipe")
est <- eb_input(
  theta_hat   = krw_firms$theta_hat_race,
  s           = krw_firms$se_race,
  unit_id     = krw_firms$firm_id,
  description = "KRW race callback gap, 97 firms"
)
est$source
length(est$theta_hat)

}
\seealso{
\code{\link[=eb]{eb()}}, \code{\link[=eb_estimate_fe]{eb_estimate_fe()}}, \code{\link[=eb_estimate_groups]{eb_estimate_groups()}}, \code{\link[=eb_simulate]{eb_simulate()}},
\code{\link[=eb_diagnose]{eb_diagnose()}}, \code{\link[=tidy.eb_estimates]{tidy.eb_estimates()}}, \code{\link[=glance.eb_estimates]{glance.eb_estimates()}}

Other eb_estimates: 
\code{\link{eb_estimate_fe}()},
\code{\link{eb_estimate_groups}()},
\code{\link{eb_simulate}()},
\code{\link{eb_standardize}()}
}
\concept{eb_estimates}
