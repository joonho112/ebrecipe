% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{plot_eb_posterior}
\alias{plot_eb_posterior}
\alias{plot.eb_posterior}
\title{Base plotting for \code{eb_posterior} objects}
\usage{
\method{plot}{eb_posterior}(
  x,
  y = NULL,
  type = c("shrinkage", "posterior", "reliability", "residuals", "qq"),
  which = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{eb_posterior} object.}

\item{y}{Unused.}

\item{type}{Plot type to construct.}

\item{which}{Optional subset of rows used by \code{type = "posterior"}.}

\item{...}{Additional graphical arguments passed to the underlying base plot.}
}
\value{
The input object, invisibly.
}
\description{
\code{plot.eb_posterior()} visualizes shrinkage output.
}
\details{
\itemize{
\item \code{"shrinkage"} compares observed estimates and posterior means
\item \code{"posterior"} draws per-unit posterior summaries for the selected rows
\item \code{"reliability"} plots shrinkage weight against standard error
\item \code{"residuals"} plots shrinkage residual structure
\item \code{"qq"} draws a QQ plot of shrinkage residuals
}

When \code{type = "posterior"}, \code{which} selects the rows to display. The current
plotting helper uses density-style displays when posterior standard
deviations are available and otherwise falls back to interval-style summaries.
}
\examples{
residual_est <- eb_input(
  theta_hat = c(-0.10, 0.05, 0.20, 0.35),
  s = c(0.20, 0.20, 0.20, 0.20)
)
prior <- eb_deconvolve(
  residual_est,
  penalty = "fixed",
  penalty_value = 0.03,
  characteristic = "male"
)
post <- eb_shrink(residual_est, prior, method = "nonparametric", unstandardize = FALSE)

plot(post, type = "shrinkage")

}
