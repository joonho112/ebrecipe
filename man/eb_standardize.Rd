% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{eb_standardize}
\alias{eb_standardize}
\title{Standardize estimates to remove precision dependence}
\usage{
eb_standardize(
  estimates,
  model = c("multiplicative", "additive"),
  diagnostic = NULL,
  start = NULL,
  ...
)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object.}

\item{model}{Precision-dependence model to use; one of \code{"multiplicative"}
or \code{"additive"}.}

\item{diagnostic}{Optional precomputed \code{eb_diagnostic}. Supplying this
reuses the fitted diagnostic models from \code{\link[=eb_diagnose]{eb_diagnose()}} instead of
refitting them.}

\item{start}{Optional starting values for the NLLS optimizer.}

\item{...}{Additional arguments reserved for future implementation.}
}
\value{
An \code{eb_estimates} object on the standardized residual scale, with
the following fields:
\describe{
\item{\code{theta_hat}}{Standardized residual-scale estimates.}
\item{\code{s}}{Standardized residual-scale standard errors.}
\item{\code{original_theta_hat}}{The pre-standardization estimates (always preserved).}
\item{\code{original_s}}{The pre-standardization standard errors.}
\item{\code{standardized}}{Logical scalar; always \code{TRUE} on the returned object.}
\item{\code{standardization_model}}{Character scalar \code{"multiplicative"} or \code{"additive"}.}
\item{\code{hyperparameters}}{Method-of-moments hyperparameters recomputed on the residual scale.}
\item{\code{attr(x, "precision_fit")}}{The fitted NLLS object with \eqn{\psi_0}, \eqn{\psi_1}, \eqn{\psi_2}, robust VCV, and uncentered pseudo-\eqn{R^2}.}
\item{\code{attr(x, "diagnostic")}}{The full \code{eb_diagnostic} bundle.}
}
\code{unit_id}, \code{n}, and \code{covariates} (if present) are passed through unchanged.
}
\description{
Transform \verb{(theta_hat, s)} to a residual scale that the deconvolution
engine treats as conditionally homoskedastic. Pick the multiplicative
model when the \code{\link[=eb_diagnose]{eb_diagnose()}} level test is significant; pick the
additive model when only the variance test is significant. Returns a new
\code{eb_estimates} object on the residual scale, preserving the originals for
later back-transformation.
}
\details{
Implements the multiplicative and additive precision-dependence models of
Walters Ch 2.6 eq. 55. The two models correspond to different stories
about how heteroskedasticity enters \eqn{\hat\theta_j}:

\itemize{
\item Multiplicative: \eqn{\hat\theta_j = \exp(\psi_1 + \psi_2 \log s_j) \cdot r_j}; both estimates and standard errors are rescaled by \eqn{\exp(\psi_1 + \psi_2 \log s_j)}.
\item Additive: \eqn{\hat\theta_j = \psi_0 + s_j^{\psi_2} r_j} after first removing a common intercept \eqn{\psi_0}; the remaining variance pattern is modelled as a function of \eqn{\log s_j}.
}

Fitted parameters are stored in \code{attr(x, "precision_fit")} and the full
diagnostic bundle in \code{attr(x, "diagnostic")}. The reported \code{r_squared} is
an uncentered Walters-style pseudo-\eqn{R^2}, NOT the centered OLS
\eqn{R^2}: for the multiplicative path,
\eqn{1 - \mathrm{SSR}/\sum \hat\theta_j^2}; for the additive path,
\eqn{1 - \mathrm{SSR}/\sum y_j^2} on the working response
\eqn{y_j = (\hat\theta_j - \psi_0)^2 - s_j^2}. These definitions match
the published Walters NLLS targets and are part of the replication
contract.

Standardization is reversible: the returned object stores
\code{original_theta_hat}, \code{original_s}, and \code{standardization_model}, so
\code{\link[=eb_shrink]{eb_shrink()}} (with \code{unstandardize = TRUE}) can map posterior summaries
back to the theta scale.
}
\section{Decision tree -- multiplicative vs. additive}{

\itemize{
\item \code{model = "multiplicative"} -- when level test from \code{\link[=eb_diagnose]{eb_diagnose()}} is significant. Transform: \eqn{r_j = \hat\theta_j / \exp(\hat\psi_1 + \hat\psi_2 \log s_j)}.
\item \code{model = "additive"} -- when only variance test is significant. Transform: \eqn{r_j = (\hat\theta_j - \hat\psi_0) / s_j^{\hat\psi_2}}.
}
}

\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = utils::head(krw_firms$theta_hat_race, 120),
  s = utils::head(krw_firms$se_race, 120)
)

diag_fit <- eb_diagnose(est, precision_models = "multiplicative")
std_est <- eb_standardize(est, model = "multiplicative", diagnostic = diag_fit)

std_est$standardized
std_est$standardization_model

}
\seealso{
\code{\link[=eb_diagnose]{eb_diagnose()}}, \code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_shrink]{eb_shrink()}},
\code{\link[=eb_change_of_variables]{eb_change_of_variables()}}, \code{\link[=eb]{eb()}}

Other eb_estimates: 
\code{\link{eb_estimate_fe}()},
\code{\link{eb_estimate_groups}()},
\code{\link{eb_input}()},
\code{\link{eb_simulate}()}
}
\concept{eb_estimates}
