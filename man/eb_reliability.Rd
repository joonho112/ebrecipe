% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{eb_reliability}
\alias{eb_reliability}
\title{Compute unit-level reliability weights}
\usage{
eb_reliability(estimates, prior)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object supplying \eqn{s_j}.}

\item{prior}{An \code{eb_prior} object supplying \eqn{\sigma_\theta^2}.}
}
\value{
A numeric vector of reliability weights \eqn{\lambda_j \in [0, 1]},
one per unit, in the order of \code{estimates$theta_hat}.
\describe{
\item{Length}{Equal to \code{length(estimates$theta_hat)}.}
\item{Range}{Each entry lies in (0, 1] for finite, strictly positive \code{s} and finite, non-negative \eqn{\sigma_\theta^2}; degenerate \eqn{\sigma_\theta^2 = 0} yields weights of 0.}
\item{Names}{Set to \code{as.character(estimates$unit_id)} when \code{unit_id} is non-\code{NULL}; otherwise the result is unnamed.}
\item{NA rule}{Never \code{NA} for valid input.}
}
}
\description{
Returns the linear empirical-Bayes reliability weights
\eqn{\lambda_j = \sigma_\theta^2 / (\sigma_\theta^2 + s_j^2)} per Walters
Ch 2.4. Larger measurement-error standard deviations imply smaller
\eqn{\lambda_j} and more aggressive shrinkage toward the prior mean.
}
\details{
Implements the linear EB reliability weight of Walters Ch 2.4:
\deqn{\lambda_j = \frac{\sigma_\theta^2}{\sigma_\theta^2 + s_j^2},}
where \eqn{\sigma_\theta^2} is read from \code{prior$hyperparameters} (via
\code{sigma_theta_sq}, falling back to \code{sigma_sq} or \code{sigma_theta^2}). Larger
measurement-error standard deviations imply smaller reliability and more
aggressive shrinkage toward the prior mean. Each \eqn{\lambda_j \in [0, 1]}.

This is the linear-EB-only counterpart of the \code{.shrinkage_weight} column
emitted by \code{\link[=eb_shrink]{eb_shrink()}} on its linear path; the nonparametric path of
\code{\link[=eb_shrink]{eb_shrink()}} does NOT collapse to a single \eqn{\lambda_j}.
}
\examples{
data("krw_firms", package = "ebrecipe")

fit <- eb(
  x = utils::head(krw_firms$theta_hat_race, 120),
  s = utils::head(krw_firms$se_race, 120),
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)

head(eb_reliability(fit$estimates, fit$prior))

}
\seealso{
\code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}}, \code{\link[=eb_mse]{eb_mse()}}, \code{\link[=eb]{eb()}}

Other eb_posterior: 
\code{\link{eb_mse}()},
\code{\link{eb_posterior_grid}()},
\code{\link{eb_shrink}()},
\code{\link{eb_shrink_conditional}()}
}
\concept{eb_posterior}
