% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-vam-prior-posterior.R
\name{plot_vam_prior_posterior}
\alias{plot_vam_prior_posterior}
\title{Plot VAM estimates, posterior means, and normal prior overlays}
\usage{
plot_vam_prior_posterior(
  x,
  method = c("unconditional", "conditional"),
  group = NULL,
  binwidth = 0.06,
  posterior_barwidth = 0.04,
  curve_range = c(-0.5, 0.5),
  n_grid = 501L,
  annotate = TRUE,
  target_id = NULL
)
}
\arguments{
\item{x}{An \code{eb_vam_fit}/\code{eb_fit}, \code{eb_estimates}, or data frame with VAM
estimates. Common columns such as \code{theta_hat}, \code{se}, \code{s}, \code{school_id},
and \code{charter} are accepted.}

\item{method}{\code{"unconditional"} for the common-prior EB plot or
\code{"conditional"} for the sector-specific prior plot.}

\item{group}{Optional grouping vector or grouping column name. When omitted,
\code{charter}, \code{sector}, or \code{group} columns are detected when available.}

\item{binwidth}{Histogram bin width on the value-added scale. Default \code{0.06}.}

\item{posterior_barwidth}{Width of filled posterior bars. Default \code{0.04}.}

\item{curve_range}{Length-two numeric range for evaluating prior curves.
Default \code{c(-0.5, 0.5)}.}

\item{n_grid}{Number of prior curve grid points. Default \code{501}.}

\item{annotate}{Whether to draw companion-style summary text. Default \code{TRUE}.}

\item{target_id}{Optional internal replication target identifier. The
recognized VAM prior/posterior target IDs are \code{fig_unconditional_eb} and
\code{fig_conditional_eb}; both are deferred Lane B contracts, not protected
companion parity targets.}
}
\value{
A \code{ggplot} object. The internal \code{eb_figure_data} object used to
build the plot is stored in \code{attr(plot, "eb_figure_data")}.
}
\description{
Draws the Lane B companion-style VAM histogram figures: open bars show raw
school value-added estimates, filled bars show empirical-Bayes posterior
means, and normal prior curve(s) are scaled to the histogram frequency axis.
Use \code{method = "unconditional"} for a single prior and
\code{method = "conditional"} for sector-specific priors.
}
\details{
The plot follows the companion \code{step5_3_run_vam.do} graph construction.
Histograms use frequency counts with default bin width \code{0.06}; posterior
bars are narrower (\code{0.04}) so the raw-estimate bars remain visible. Prior
density curves are multiplied by the relevant school count and bin width so
that they sit on the same frequency scale as the histograms.

With bundled package data, this helper targets the companion simulation
figures. The deferred target IDs \code{fig_unconditional_eb} and
\code{fig_conditional_eb} are Lane B contracts tied to the \code{vam_schools} source
shape and caption-number receipts. They are not protected Boston parity and
do not claim to reproduce restricted Boston-school records that cannot be
shipped with the package.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("vam_schools", package = "ebrecipe")
plot_vam_prior_posterior(vam_schools, method = "unconditional")
plot_vam_prior_posterior(vam_schools, method = "conditional")
plot_vam_prior_posterior(
  vam_schools,
  method = "unconditional",
  target_id = "fig_unconditional_eb"
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_vam_truth_shrinkage]{plot_vam_truth_shrinkage()}}, \code{\link[=eb_vam]{eb_vam()}}, \code{\link[=eb_simulate]{eb_simulate()}}
}
