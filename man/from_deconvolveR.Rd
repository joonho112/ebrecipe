% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-deconvolveR.R
\name{from_deconvolveR}
\alias{from_deconvolveR}
\title{Wrap a deconvolveR result as an eb_prior}
\usage{
from_deconvolveR(object, ...)
}
\arguments{
\item{object}{A \code{deconvolveR::deconv()} result list. Must contain a \code{stats}
component with columns named \code{theta} (or \code{tau}) and \code{g} (or \code{tg}).
Optional cached fields (\code{mle}, \code{cov}, \code{Q}, \code{P}, \code{S}, \code{cov.g}, \code{loglik},
\code{statsFunction}) are preserved on the resulting prior when present.}

\item{...}{Currently recognized: \code{sigma} (positive numeric scalar, default
\code{1}) for converting standardized support to the \eqn{\theta} scale under
homoskedastic normal errors; \code{scale} (one of \code{"theta"}/\code{"z"}, default
\code{"theta"}) for choosing the support scale stored on the prior. Other
arguments are reserved.}
}
\value{
An \code{eb_prior} object with \code{method = "deconvolver"} and the
following public fields:
\describe{
\item{\code{method}}{Character scalar -- always \code{"deconvolver"} for this constructor.}
\item{\code{alpha}}{Numeric vector -- imported \code{mle} (spline coefficients); \code{numeric(0)} when absent.}
\item{\code{support}}{Numeric vector -- grid points in the chosen \code{scale}. When \code{scale = "theta"}, equal to \code{tau * sigma}; when \code{scale = "z"}, equal to \code{tau}. Strictly increasing; never \code{NA}.}
\item{\code{density}}{Numeric vector -- normalized prior density on \code{support}, integrating to 1. Never \code{NA}.}
\item{\code{V}}{Numeric matrix or \code{NULL} -- the deconvolveR \code{cov} matrix when present.}
\item{\code{hyperparameters}}{Named list with discrete moments \code{mu} (\eqn{\sum_k \tau_k g_k}), \code{sigma_theta}, \code{sigma_theta_sq}. Recomputed from imported support and mass; never \code{NA}.}
\item{\code{scale}}{Character scalar -- \code{"theta"} or \code{"z"} mirroring the \code{scale} argument.}
\item{\code{spline_info}}{Named list caching deconvolveR-specific quantities (\code{backend = "deconvolveR"}, \code{deconvolveR_tau}, \code{deconvolveR_mass}, \code{deconvolveR_Q}, \code{deconvolveR_P}, \code{deconvolveR_S}, \code{deconvolveR_cov_g}, \code{deconvolveR_loglik}, \code{deconvolveR_stats_function}, \code{sigma}) so \code{\link[=as_deconvolveR]{as_deconvolveR()}} can reconstruct the original list losslessly.}
}
}
\description{
Coerces the list returned by \code{deconvolveR::deconv()} into an \code{ebrecipe}
\code{eb_prior} so it can be plotted, summarized, fed to \code{\link[=eb_shrink]{eb_shrink()}} for
posterior summaries, or compared against native \code{ebrecipe} priors.
Hyperparameters are recomputed from the imported support and mass; cached
deconvolveR quantities are stashed in \code{spline_info} so the inverse
coercion \code{\link[=as_deconvolveR]{as_deconvolveR()}} is lossless.
}
\details{
This bridge assumes the homoskedastic-normal convention used by the
\code{method = "deconvolver"} path in \code{\link[=eb_deconvolve]{eb_deconvolve()}}: the deconvolveR support
\code{tau} is on the standardized scale \eqn{z = \hat\theta / \sigma}. With
\code{scale = "theta"}, the function rescales support to the \eqn{\theta} scale
by multiplying by \code{sigma}; with \code{scale = "z"} it keeps the standardized
scale (Walters Ch 5.4 -- Efron 2016 discrete-spline formulation).

Hyperparameters are recomputed as the discrete moments
\deqn{\mu = \sum_k \tau_k g_k, \quad
      \sigma_\theta^2 = \sum_k (\tau_k - \mu)^2 g_k}
so the resulting prior carries first- and second-moment summaries
compatible with the native logspline backend. The native \code{ebrecipe}
optimization context (e.g., the variance-match penalty trace) is NOT
reconstructed -- this is a comparison object, not proof that the two
backends fit identical models.
}
\section{Decision tree -- when to use which prior bridge}{

\itemize{
\item Use \code{\link[=from_deconvolveR]{from_deconvolveR()}} to wrap a \code{deconvolveR::deconv()} result AS an \code{ebrecipe} prior.
\item Use \code{\link[=as_deconvolveR]{as_deconvolveR()}} for the inverse coercion.
\item Use \code{\link[=eb_deconvolve]{eb_deconvolve()}} with \code{method = "deconvolver"} for end-to-end fitting through deconvolveR.
}
}

\examples{
# Wrap a tiny synthetic deconvolveR result.
raw <- list(
  mle = c(0.1, -0.2),
  stats = cbind(
    theta = c(-1, 0, 1),
    g     = c(0.2, 0.6, 0.2)
  )
)
prior <- from_deconvolveR(raw, sigma = 0.5, scale = "theta")
prior$scale
prior$hyperparameters$mu
range(prior$support)

# Round-trip back to a deconvolveR-shaped structure.
bridge <- as_deconvolveR(prior)
identical(prior$scale, "theta")

}
\seealso{
\code{\link[=as_deconvolveR]{as_deconvolveR()}}, \code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_shrink]{eb_shrink()}},
\code{\link[=tidy.eb_prior]{tidy.eb_prior()}}, \code{\link[=glance.eb_prior]{glance.eb_prior()}}, \code{\link[=autoplot.eb_prior]{autoplot.eb_prior()}}

Other eb_prior: 
\code{\link{as_deconvolveR}()},
\code{\link{eb_change_of_variables}()}
}
\concept{eb_prior}
