% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-groups.R
\name{eb_estimate_groups}
\alias{eb_estimate_groups}
\title{Estimate one treatment slope per group via within-group OLS}
\usage{
eb_estimate_groups(
  formula,
  data,
  cluster = NULL,
  se_type = c("classical", "HC1", "HC2", "stata"),
  weights = NULL,
  min_obs = 2L,
  na.action = na.omit,
  ...
)
}
\arguments{
\item{formula}{Three-part formula \code{outcome ~ treatment + covariates | group_id}. The first right-hand-side term is treated as the estimand of
interest and must map to exactly one coefficient within each group;
factor/interaction/spline expansions on the treatment term are out of
contract.}

\item{data}{Data frame containing all variables in \code{formula}, plus
\code{cluster} and \code{weights} if supplied.}

\item{cluster}{Optional one-sided formula naming a single clustering
variable, e.g. \code{~ job_id}. Required when \code{se_type = "stata"}.}

\item{se_type}{Character scalar; SE estimator. \code{"stata"} applies the
small-sample correction \eqn{(G/(G-1)) \cdot ((N-1)/(N-k))} and requires
\code{cluster}. Groups with only one retained cluster under \code{"stata"} fall
back to \code{"HC1"} for that group, with a warning.}

\item{weights}{Optional numeric observation weights (length \code{nrow(data)})
or a length-1 character naming a weight column. Weighted robust or
clustered SEs are not implemented in the current public path.}

\item{min_obs}{Integer; minimum observations required per group. Groups
with fewer rows are dropped with a warning. Default \code{2L}.}

\item{na.action}{Function applied to drop missing rows prior to fitting,
e.g. \code{na.omit}. Default \code{na.omit}.}

\item{...}{Reserved for future arguments.}
}
\value{
An \code{eb_estimates} object with \code{source = "group_slope"} and the
following public fields:
\describe{
\item{\code{theta_hat}}{Numeric vector -- per-group treatment slopes \eqn{\hat\theta_j}, one entry per retained group. Never \code{NA} (groups with unestimable \eqn{\hat\theta_j} are dropped, not returned as \code{NA}).}
\item{\code{s}}{Numeric vector -- per-group standard errors from the chosen \code{se_type}. Never \code{NA}.}
\item{\code{unit_id}}{Vector of group identifiers in retention order. Never \code{NA}.}
\item{\code{n}}{Integer vector -- per-group row counts when \code{cluster} is \code{NULL}; per-group unique cluster counts when \code{cluster} is supplied. Never \code{NA} for retained groups.}
\item{\code{covariates}}{\code{NULL} for this wrapper (no per-unit covariate carry-through).}
\item{\code{source}}{Character scalar -- always \code{"group_slope"}.}
\item{\code{description}}{Character scalar -- records \code{se_type}, presence of \code{cluster}, and dropped groups.}
}
}
\description{
Fits one OLS regression per group and extracts a single treatment
coefficient per group as \eqn{\hat\theta_j}, with the matching standard
error \eqn{s_j} from a chosen SE estimator (classical, HC1, HC2, or
Stata-style cluster). Returns the per-group estimates as an \code{eb_estimates}
object -- the canonical input layer for KRW-style discrimination
workflows (Walters Ch 4.2).
}
\details{
Within each group \eqn{j}, the function fits
\deqn{y_{ij} = \alpha_j + \theta_j d_{ij} + x_{ij}^\top \beta_j + \varepsilon_{ij}}
using \code{lm()}, then extracts \eqn{\hat\theta_j} (the coefficient on the first
right-hand-side term) and the corresponding \eqn{s_j} under the requested
SE rule. The package then treats \eqn{\hat\theta_j \sim N(\theta_j, s_j^2)}
(Walters Ch 2.1 eq. 8) as the EB input contract. This makes the function
well suited to "one treatment contrast per group" workflows (e.g.
Kline-Rose-Walters callback gaps by firm; Walters Ch 4.2 eq. 12-14) but
not to arbitrary multi-parameter grouped modeling.

Missing-value filtering is applied up front through \code{na.action} across the
outcome, right-hand-side variables, grouping variable, and any supplied
cluster or weight inputs. Group fitting then uses \code{na.fail} so any
remaining \code{NA} is treated as a programming error.

Groups are dropped with warnings under three conditions: (1) fewer than
\code{min_obs} rows after filtering; (2) the target treatment coefficient is not
estimable within the group due to collinearity or lack of identifying
variation; (3) \code{se_type = "stata"} with a single retained cluster, where
the group falls back to HC1. If every group is dropped, the function
errors.
}
\section{Decision tree -- when to use which input wrapper}{

\itemize{
\item Use \code{\link[=eb_estimate_groups]{eb_estimate_groups()}} for one treatment slope per group (e.g., per-firm hire-rate gap).
\item Use \code{\link[=eb_estimate_fe]{eb_estimate_fe()}} for unit fixed-effect VAM workflows.
\item Use \code{\link[=eb_input]{eb_input()}} when \eqn{\hat\theta_j, s_j} were computed outside \code{ebrecipe}.
\item Use \code{\link[=eb_simulate]{eb_simulate()}} for synthetic data with known truth.
}
}

\section{Formula contract}{

The first right-hand-side term before \code{|} is treated as the estimand of
interest. It must map to exactly one coefficient within each group. Terms
that expand into multiple coefficients, such as some factors, interactions,
or spline bases, are therefore outside the supported contract for the target
treatment effect in the current public path.
}

\section{Standard errors}{

\code{classical}, \code{HC1}, and \code{HC2} are applied group by group. \code{se_type = "stata"}
requires a one-sided clustering formula and applies the documented Stata-like
small-sample correction. Because groups with only one retained cluster fall
back to \code{HC1}, standard-error behavior may differ across groups within the
same call.
}

\section{Group dropping}{

Groups are dropped with warnings if they fall below \code{min_obs} after
preprocessing or if the target treatment effect is not estimable because of
within-group collinearity or lack of identifying variation. If every group is
dropped, the function errors.
}

\examples{
# Per-school slope of y on x using the bundled VAM micro-data.
data("vam_simulated", package = "ebrecipe")
est <- eb_estimate_groups(
  y ~ x | school_id,
  data    = vam_simulated,
  se_type = "classical"
)
length(est$theta_hat)
head(est$s)

}
\seealso{
\code{\link[=eb_input]{eb_input()}}, \code{\link[=eb_estimate_fe]{eb_estimate_fe()}}, \code{\link[=eb_simulate]{eb_simulate()}},
\code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb]{eb()}},
\code{\link[=tidy.eb_estimates]{tidy.eb_estimates()}}, \code{\link[=glance.eb_estimates]{glance.eb_estimates()}}

Other eb_estimates: 
\code{\link{eb_estimate_fe}()},
\code{\link{eb_input}()},
\code{\link{eb_simulate}()},
\code{\link{eb_standardize}()}
}
\concept{eb_estimates}
