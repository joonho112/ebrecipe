% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconv-engine.R
\name{eb_deconvolve}
\alias{eb_deconvolve}
\title{Estimate an empirical Bayes prior by deconvolution}
\usage{
eb_deconvolve(
  estimates,
  theta_hat = NULL,
  s = NULL,
  method = c("logspline", "deconvolver"),
  n_knots = 5,
  grid_size = 1000,
  grid_range = NULL,
  penalty = c("variance_match", "fixed", "none"),
  penalty_value = NULL,
  mean_constraint = TRUE,
  mu = NULL,
  sigma_theta = NULL,
  control = NULL,
  ...
)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object. When produced by
\code{\link[=eb_estimate_fe]{eb_estimate_fe()}} or \code{\link[=eb_standardize]{eb_standardize()}}, the stored \code{theta_hat} and \code{s}
are already on the standardized residual scale used by the logspline
engine.}

\item{theta_hat}{Optional estimate vector on the deconvolution scale. For
direct \code{method = "logspline"} calls, this currently means pre-standardized
residuals \code{r}, not raw theta-scale estimates.}

\item{s}{Optional standard-error vector on the deconvolution scale. For
direct \code{method = "logspline"} calls, this currently means residual-scale
standard errors \code{s_r}.}

\item{method}{Prior family or backend choice.}

\item{n_knots}{Number of spline basis functions.}

\item{grid_size}{Number of grid points used for the support.}

\item{grid_range}{Optional support range override on the same scale as
\code{theta_hat}. For the implemented logspline path, this is the standardized
residual scale \code{r}.}

\item{penalty}{Penalty handling rule.}

\item{penalty_value}{Optional fixed penalty value.}

\item{mean_constraint}{Logical; whether to impose the mean constraint.}

\item{mu}{Optional prior mean override.}

\item{sigma_theta}{Optional prior standard deviation override.}

\item{control}{Optional \code{eb_control} object for deconvolution tuning. When
\code{control$replication_mode = TRUE}, the replication settings stored in
\code{control} override conflicting direct deconvolution arguments.}

\item{...}{Additional arguments reserved for future implementation. The
current logspline path also recognizes \code{characteristic}, \code{target_mean},
\code{psi_1}, \code{psi_2}, \code{original_s}, \code{penalty_grid}, \code{seed}, and \code{optimizer}.}
}
\value{
An \code{eb_prior} object.
}
\description{
\code{eb_deconvolve()} estimates the prior distribution used by the package's
empirical-Bayes shrinkage workflow. The current native implementation is the
Walters-style log-spline deconvolution engine, parameterized on the
standardized residual scale \code{r}.
}
\details{
\code{eb_deconvolve()} currently implements the native \code{logspline} engine and a
comparison-oriented \code{deconvolver} bridge for homoskedastic normal errors.

The native \code{logspline} path does not auto-standardize raw theta-scale inputs.
If you call \code{eb_deconvolve()} directly on raw vectors in that mode, you must
supply pre-standardized residual-scale inputs \verb{(theta_hat = r, s = s_r)}.

The optional \code{method = "deconvolver"} path is intentionally narrower. It is
provided as a comparison bridge to Efron's \code{deconvolveR} package and
currently supports only homoskedastic normal errors.

If \code{estimates} comes from \code{\link[=eb_standardize]{eb_standardize()}}, \code{eb_deconvolve()} now recovers
the stored precision-dependence metadata automatically so that downstream
calls such as \code{\link[=eb_shrink]{eb_shrink()}} can unstandardize posterior means without
repeating \code{psi_1}, \code{psi_2}, or \code{original_s} by hand.

\code{characteristic = "white"} and \code{characteristic = "male"} currently affect
only the residual-scale target mean/support conventions and, when \code{psi_1},
\code{psi_2}, and \code{original_s} are supplied, the theta-scale pushforward summary.
The same metadata are also stored in \code{prior$spline_info} so downstream
helpers can recover the theta scale for validated unstandardization paths.

When \code{control$replication_mode = TRUE}, \code{eb_deconvolve()} treats the control
object as a hard override for the Walters replication defaults, including the
spline basis size, grid size, mean-constraint rule, penalty search grid, and
optimizer settings. Conflicting direct arguments are warned on and then
replaced by the replication settings.

A future higher-level direct-call interface may still add automatic
standardization for raw theta-scale vectors when sufficient metadata are
available.
}
\examples{
# Direct calls currently expect pre-standardized residual-scale inputs.
residual_est <- eb_input(
  theta_hat = c(-0.10, 0.05, 0.20, 0.35),
  s = c(0.20, 0.20, 0.20, 0.20)
)

prior <- eb_deconvolve(
  estimates = residual_est,
  penalty = "fixed",
  penalty_value = 0.03,
  characteristic = "male"
)

prior$scale
prior$penalty_value

}
\seealso{
\code{\link[=eb_standardize]{eb_standardize()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_change_of_variables]{eb_change_of_variables()}}
}
