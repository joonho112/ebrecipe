% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconv-delta-method.R
\name{eb_delta_method}
\alias{eb_delta_method}
\title{Compute delta-method standard errors for prior moments}
\usage{
eb_delta_method(prior, functions = c("mean", "variance", "sd"), ...)
}
\arguments{
\item{prior}{An \code{eb_prior} object with sandwich VCV in \verb{$V}.}

\item{functions}{Moments to evaluate.}

\item{...}{Additional arguments reserved for future implementation.}
}
\value{
A data frame with columns \code{moment}, \code{estimate}, and \code{se}.
}
\description{
Compute delta-method standard errors for prior moments
}
\details{
\code{eb_delta_method()} is currently a post-estimation summary for r-scale priors
that carry a sandwich covariance matrix for the free spline coefficients.

Ordinary output from \code{\link[=eb_deconvolve]{eb_deconvolve()}} does not include this covariance
matrix: the current public contract is that \code{prior$V} is \code{NULL} unless a
sandwich layer has explicitly attached a numeric VCV for the free spline
coefficients in \code{prior$alpha}.

The returned standard errors are conditional on the supplied sandwich VCV in
\code{prior$V}, and therefore conditional on the selected penalty parameter and
any upstream precision-dependence estimates used to construct the prior. They
do not propagate uncertainty from penalty selection or first-stage
standardization.

Priors transformed with \code{\link[=eb_change_of_variables]{eb_change_of_variables()}} intentionally do not carry
forward the original sandwich VCV. If delta-method standard errors are
needed, call \code{eb_delta_method()} on the original r-scale prior before
applying the change of variables.
}
\seealso{
\code{\link[=eb_deconvolve]{eb_deconvolve()}}, \code{\link[=eb_change_of_variables]{eb_change_of_variables()}}
}
