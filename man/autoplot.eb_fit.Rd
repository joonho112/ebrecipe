% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{autoplot.eb_fit}
\alias{autoplot.eb_fit}
\title{Autoplot an \code{eb_fit} object with ggplot2}
\usage{
autoplot.eb_fit(
  object,
  type = c("all", "results", "diagnostics", "prior", "mixing", "posterior", "shrinkage",
    "shrinkage_comparison", "reliability", "histogram", "fdr", "pvalue", "qvalue",
    "frontier", "decision"),
  characteristic = "estimate",
  scale = c("theta", "r"),
  metric = c("p", "q"),
  grid = NULL,
  comparison = c("linear", "precision_adjusted"),
  combine = c("patchwork", "list"),
  ...
)
}
\arguments{
\item{object}{An \code{eb_fit} object as produced by \code{\link[=eb]{eb()}}, \code{\link[=eb_test]{eb_test()}}, or
\code{\link[=eb_vam]{eb_vam()}}.}

\item{type}{Plot type to construct. One of:
\describe{
\item{\code{"all"}}{Falls back to \code{\link[=plot.eb_fit]{plot.eb_fit()}} with \code{type = "diagnostic"} (a multi-panel base-graphics overview). Returns \code{invisible(NULL)}.}
\item{\code{"results"}}{High-level results dashboard from \code{\link[=plot_results]{plot_results()}}.}
\item{\code{"diagnostics"}}{High-level diagnostic dashboard from \code{\link[=plot_diagnostics]{plot_diagnostics()}}.}
\item{\code{"prior"} or \code{"mixing"}}{Companion-quality prior/mixing plot from \code{\link[=plot_mixing_distribution]{plot_mixing_distribution()}}.}
\item{\code{"posterior"}}{Companion-quality posterior overlay from \code{\link[=plot_posterior_overlay]{plot_posterior_overlay()}}.}
\item{\code{"shrinkage"}}{Backward-compatible shrinkage scatter of observed estimates against posterior means.}
\item{\code{"shrinkage_comparison"}}{Companion-style nonparametric-versus-linear comparison from \code{\link[=plot_shrinkage_comparison]{plot_shrinkage_comparison()}} when comparison columns are available.}
\item{\code{"reliability"}}{Backward-compatible scatter of standard error against shrinkage weight.}
\item{\code{"histogram"}}{Histogram of the observed estimates \eqn{\hat\theta_j}.}
\item{\code{"fdr"}, \code{"pvalue"}, or \code{"qvalue"}}{FDR histogram from \code{\link[=plot_fdr_histogram]{plot_fdr_histogram()}}.}
\item{\code{"frontier"}}{Decision frontier from \code{\link[=plot_decision_frontier]{plot_decision_frontier()}}; requires \code{grid}.}
\item{\code{"decision"}}{High-level decision dashboard from \code{\link[=plot_decision]{plot_decision()}}; requires \code{grid}.}
}}

\item{characteristic}{Length-one plot label passed to companion plot
helpers. Use labels such as \code{"white"} or \code{"male"} for Walters
discrimination figures.}

\item{scale}{Prior/mixing scale for \code{type = "prior"} or \code{"mixing"}.}

\item{metric}{Histogram metric for \code{type = "fdr"}; ignored by the explicit
aliases \code{type = "pvalue"} and \code{type = "qvalue"}.}

\item{grid}{Posterior decision-surface grid for \code{type = "frontier"} and
\code{type = "decision"}. It is never generated automatically.}

\item{comparison}{Shrinkage comparator for \code{type = "shrinkage_comparison"}.}

\item{combine}{Dashboard return mode passed to \code{\link[=plot_results]{plot_results()}},
\code{\link[=plot_diagnostics]{plot_diagnostics()}}, and \code{\link[=plot_decision]{plot_decision()}}.}

\item{...}{Additional arguments forwarded to the selected plot helper, or to
the base plotting fallback when \code{ggplot2} is unavailable.}
}
\value{
A \code{ggplot} object for single-panel \code{type} values, a patchwork/list
dashboard for workflow \code{type} values, or \code{invisible(NULL)} when delegating
to the base plotting fallback.
}
\description{
S3 \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} method for the package's monolithic \code{eb_fit}
container (produced by \code{\link[=eb]{eb()}}, \code{\link[=eb_test]{eb_test()}}, or \code{\link[=eb_vam]{eb_vam()}}). The method keeps
the original simple diagnostic views while adding routes into the
companion-quality plot helpers and high-level workflow dashboards.
}
\details{
\code{autoplot.eb_fit()} is now the ergonomic bridge from fitted EB workflows to
the explicit companion plot functions. It preserves the old \code{"all"} base
diagnostic fallback and the simple \code{"shrinkage"}, \code{"reliability"}, and
\code{"histogram"} views, while explicit companion routes require the same
semantic inputs as their underlying helpers. In particular, frontier and
decision dashboards require a supplied posterior grid. These routes are
ergonomic workflow views by default; exact Lane A companion parity should use
the specialized helpers directly with protected \code{target_id} values and
matching source receipts. VAM autoplot routes remain deferred or
simulation-only according to the underlying VAM helpers.
}
\section{N-18 binding rationale}{

Per redesign decision N-18, \code{autoplot.eb_fit} is the SINGLE statically
exported S3 method registered against \code{ggplot2::autoplot} in NAMESPACE
(\code{export(autoplot.eb_fit)}). All other \verb{autoplot.*} methods in the package
are runtime-registered at \code{.onLoad()} via the in-house
\code{.eb_register_s3_method()} helper in \code{R/zzz.R}. The static export is kept
for v1 backward compatibility -- in v1, \code{ebrecipe::autoplot.eb_fit()} was
directly callable by name, and downstream code (including the Walters
(2024) replication companion) relies on that binding. The
runtime-registration pattern is preferred for new methods because it lets
\code{ggplot2} stay in \code{Suggests} without forcing a hard \code{Depends} on it
(DEC-124-1 "zero CRAN deps").
}

\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  data("krw_firms", package = "ebrecipe")
  fit <- eb(
    x = utils::head(krw_firms$theta_hat_race, 80),
    s = utils::head(krw_firms$se_race, 80),
    method = "linear",
    control = eb_control(standardize = FALSE, precision_model = "none")
  )
  p <- ggplot2::autoplot(fit, type = "shrinkage")
  print(p)

  panels <- ggplot2::autoplot(
    fit,
    type = "results",
    characteristic = "white",
    combine = "list"
  )
  names(panels)
}
}
}
\seealso{
\code{\link[=plot.eb_fit]{plot.eb_fit()}} for the full base-graphics catalogue including
classification-dependent views; \code{\link[=eb]{eb()}}, \code{\link[=eb_test]{eb_test()}}, \code{\link[=eb_vam]{eb_vam()}} for the
workflows that produce \code{eb_fit} objects; \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} for the
generic.
}
\concept{eb_autoplot}
