% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vam.R
\name{eb_vam}
\alias{eb_vam}
\title{Run the value-added model workflow}
\usage{
eb_vam(
  formula,
  data,
  se_source = c("analytical", "vce_matrix"),
  vce_matrix = NULL,
  conditional_on = NULL,
  method = "linear",
  control = eb_control(),
  ...
)
}
\arguments{
\item{formula}{A two-part formula \code{outcome ~ covariates | school_id}. With
\code{se_source = "analytical"}, this is the pooled student-level VAM
regression used to estimate school effects. With
\code{se_source = "vce_matrix"}, the left-hand side is interpreted as an
already-estimated school effect stored in a school-level table.}

\item{data}{A data frame. With \code{se_source = "analytical"}, \code{data} should be
student-level and \code{formula} should describe the pooled VAM regression.
With \code{se_source = "vce_matrix"}, \code{data} should contain one row per
school with imported unit estimates in the left-hand-side column. Import
mode does NOT re-estimate school effects.}

\item{se_source}{Standard-error source: \code{"analytical"} to fit FE from
student-level data; \code{"vce_matrix"} to import a precomputed VCE matrix.}

\item{vce_matrix}{Optional precomputed variance-covariance matrix for
school-level import mode.}

\item{conditional_on}{Optional one-sided formula defining school-level
covariates for conditional linear EB, e.g. \code{~ charter}. In analytical
mode, each listed covariate must be constant within school. When \code{NULL},
the workflow uses unconditional linear shrinkage; otherwise it switches
to \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}}.}

\item{method}{EB method used downstream. Only \code{"linear"} is currently
implemented for the VAM pipeline.}

\item{control}{An \code{\link[=eb_control]{eb_control()}} configuration object. Stored on the
returned fit and forwarded to the conditional-linear path when
\code{conditional_on} is supplied.}

\item{...}{Additional arguments reserved for future implementation.}
}
\value{
An \code{eb_vam_fit} object: a list with class
\code{c("eb_vam_fit", "eb_fit", "list")} and the following fields:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{method}}{Character scalar; \code{"linear"} (unconditional) or \code{"conditional_linear"} (when \code{conditional_on} is supplied).}
\item{\code{estimates}}{The school-level \code{eb_estimates} produced by \code{\link[=eb_estimate_fe]{eb_estimate_fe()}} (analytical) or built from \code{vce_matrix} (import).}
\item{\code{prior}}{An \code{eb_prior} linear normal-prior summary; for the conditional path, the prior reported by \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}}.}
\item{\code{posterior}}{An \code{eb_posterior} object with linear or conditional-linear shrinkage output.}
\item{\code{hyperparameters}}{Named list always containing an \code{unconditional} block, plus a \code{conditional} block when \code{conditional_on} is supplied.}
\item{\code{log_likelihood}}{Numeric scalar; typically \code{NA_real_} (linear path does not maximize a marginal likelihood).}
\item{\code{convergence}}{Named list recording \code{converged}, \code{stage = "eb_vam"}, and the resolved \code{se_source}.}
\item{\code{precision_dep}}{An \code{eb_diagnostic} placeholder for object consistency; the VAM path does not standardize.}
\item{\code{classification}}{Always \code{NULL} for the current VAM path.}
\item{\code{control}}{The validated \code{eb_control} object.}
}
}
\description{
Linear empirical-Bayes value-added pipeline. Combines school-effect
estimation or import, method-of-moments normal-prior fitting, and either
unconditional or conditional linear shrinkage in a single wrapper. The
VAM-flavored sibling of \code{\link[=eb]{eb()}}.
}
\details{
Implements the linear VAM workflow of Walters Ch 4. The pipeline is
deliberately narrower than \code{\link[=eb]{eb()}}: no nonparametric deconvolution, no
decision-surface grid, no FDR classification. The prior is the
method-of-moments normal
\eqn{\theta_j \sim N(\hat\mu, \hat\sigma_\theta^2)} (unconditional;
Walters Ch 2.4) or
\eqn{\theta_j \sim N(Z_j' \hat\mu, \hat\sigma_r^2)} (conditional; Walters
Ch 4.3, where \eqn{\hat\sigma_r^2} is the residual signal variance after
partialling out \eqn{Z_j}).

Unconditional linear shrinkage moves each school's estimate toward the
global mean using weight \eqn{w_j = \sigma_\theta^2 / (\sigma_\theta^2 + s_j^2)}.
Conditional linear shrinkage replaces the global mean with a
covariate-dependent prior mean (e.g., sector mean when
\code{conditional_on = ~ charter}).

The returned \code{precision_dep} component is included for object consistency
with \code{\link[=eb]{eb()}}, but the VAM path does not fit or apply precision-dependence
standardization (Walters Ch 2.6 is not invoked here).
}
\section{Decision tree -- unconditional vs. conditional VAM}{

\itemize{
\item \code{conditional_on = NULL} (default): unconditional prior \eqn{N(\hat\mu, \hat\sigma_\theta^2)}; all schools shrink to the global mean.
\item \code{conditional_on = ~ charter}: conditional prior \eqn{N(Z_j' \hat\mu, \hat\sigma_r^2)}; schools shrink to sector mean.
}
}

\section{Decision tree -- SE source}{

\itemize{
\item \code{se_source = "analytical"} (default): student-level data; SEs computed from FE regression.
\item \code{se_source = "vce_matrix"}: school-level data + VCE matrix; SEs imported.
}
}

\examples{
data("vam_simulated", package = "ebrecipe")

fit_analytic <- eb_vam(y ~ x | school_id, data = vam_simulated)
fit_analytic$method
head(fit_analytic$posterior[, c(".theta_hat", ".posterior_mean")])

data("vam_schools", package = "ebrecipe")

fit_imported <- eb_vam(
  theta_hat ~ 1 | school_id,
  data = vam_schools,
  se_source = "vce_matrix",
  vce_matrix = diag(vam_schools$se^2),
  conditional_on = ~ charter
)
fit_imported$method
head(fit_imported$posterior[, c(".prior_mean", ".posterior_mean")])

}
\seealso{
\code{\link[=eb_estimate_fe]{eb_estimate_fe()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}},
\code{\link[=eb_simulate]{eb_simulate()}},
\code{\link[=tidy.eb_fit]{tidy.eb_fit()}}, \code{\link[=glance.eb_fit]{glance.eb_fit()}}, \code{\link[=augment.eb_fit]{augment.eb_fit()}},
\code{\link[=autoplot.eb_vam_fit]{autoplot.eb_vam_fit()}}

Other eb_fit: 
\code{\link{eb}()},
\code{\link{eb_control}()},
\code{\link{eb_test}()}
}
\concept{eb_fit}
