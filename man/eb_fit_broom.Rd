% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R, R/class-broom.R, R/fortify.R
\name{autoplot.eb_vam_fit}
\alias{autoplot.eb_vam_fit}
\alias{eb_fit_broom}
\alias{tidy.eb_fit}
\alias{glance.eb_fit}
\alias{augment.eb_fit}
\alias{fortify.eb_fit}
\title{Broom and ggplot2 methods for \code{eb_fit} objects}
\usage{
autoplot.eb_vam_fit(
  object,
  type = c("all", "prior_posterior", "vam_prior_posterior", "unconditional",
    "conditional", "truth", "truth_shrinkage", "vam_truth_shrinkage", "results",
    "diagnostics", "prior", "mixing", "posterior", "shrinkage", "shrinkage_comparison",
    "reliability", "histogram", "fdr", "pvalue", "qvalue", "frontier", "decision"),
  vam_method = NULL,
  truth = NULL,
  ...
)

tidy.eb_fit(x, conf.int = FALSE, conf.level = 0.95, ...)

glance.eb_fit(x, ...)

augment.eb_fit(x, ...)

fortify.eb_fit(model, data, ...)
}
\arguments{
\item{object}{An \code{eb_vam_fit} object.}

\item{type}{Plot type passed through to \code{autoplot.eb_fit()}. See
\code{\link[=autoplot.eb_fit]{autoplot.eb_fit()}} for choices.}

\item{vam_method}{VAM prior/posterior plot method, either
\code{"unconditional"} or \code{"conditional"}. When \code{NULL}, the method is inferred
from \code{object$method}.}

\item{truth}{Required for truth plot types; data frame, student-level
simulation data, or \code{eb_sim} object passed to \code{\link[=plot_vam_truth_shrinkage]{plot_vam_truth_shrinkage()}}.}

\item{...}{Forwarded to \code{tidy.eb_fit()}.}

\item{x}{An \code{eb_fit} object.}

\item{conf.int}{Logical; whether to append confidence intervals in \code{tidy()}.}

\item{conf.level}{Confidence level used when \code{conf.int = TRUE}.}

\item{model}{An \code{eb_fit} object.}

\item{data}{Unused (kept for ggplot2 fortify generic signature).}
}
\value{
A \code{ggplot} object (or, when \code{type = "all"}, the diagnostic
collection that \code{autoplot.eb_fit()} returns).

\code{tidy()} returns a unit-level data frame. \code{glance()} returns a
one-row data frame. \code{augment()} returns the merged fit table augmented with
\code{.fitted}, \code{.resid}, and \code{.hat}.

A \code{data.frame} (the result of \code{tidy.eb_fit(model, ...)}).
}
\description{
Delegates to \code{autoplot.eb_fit()} (since \code{eb_vam_fit} is a subclass of
\code{eb_fit}) and attaches a VAM-specific subtitle. Most of the plotting
work -- prior, shrinkage map, reliability -- is inherited from the
static \code{autoplot.eb_fit()} method.

These methods provide broom-style access to fitted EB objects.

Equivalent to \code{as.data.frame(tidy(model))}. Lets \code{eb_fit} objects flow
directly into \code{ggplot2::ggplot(model, ...)} via the fortify-based data
conversion.
}
\details{
\code{autoplot.eb_vam_fit()} is an ergonomic workflow route. VAM prior/posterior
target IDs remain deferred Lane B contracts, and truth routes are
simulation-only; they do not mint protected restricted-Boston parity.

\itemize{
\item \code{tidy()} returns one row per unit with observed estimates, posterior
summaries, and optional classification columns
\item \code{glance()} returns a one-row fit summary
\item \code{augment()} returns the merged fit table plus \code{.fitted}, \code{.resid}, and
\code{.hat}
}

Classification columns are appended only when the fit carries
classification output. Confidence-interval columns are added to \code{tidy()}
only when \code{conf.int = TRUE}.
}
\examples{
data("krw_firms", package = "ebrecipe")
krw_small <- utils::head(krw_firms, 80)

fit <- eb(
  x = krw_small$theta_hat_race,
  s = krw_small$se_race,
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)

if (requireNamespace("broom", quietly = TRUE)) {
  broom::tidy(fit)
  broom::glance(fit)
  head(broom::augment(fit))
}

}
