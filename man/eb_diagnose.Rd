% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{eb_diagnose}
\alias{eb_diagnose}
\title{Diagnose precision dependence in noisy estimates}
\usage{
eb_diagnose(
  estimates,
  x = NULL,
  s = NULL,
  tests = c("level", "variance"),
  precision_models = c("multiplicative", "additive"),
  ...
)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object (preferred). Mutually exclusive
with \code{x} / \code{s}.}

\item{x}{Optional raw estimate vector \eqn{\hat\theta_j}, used when
\code{estimates} is omitted.}

\item{s}{Optional raw standard-error vector \eqn{s_j} (strictly positive),
used when \code{estimates} is omitted.}

\item{tests}{Diagnostic regression types to run. \code{"level"} regresses
\eqn{\hat\theta_j} on \eqn{\log s_j}. \code{"variance"} regresses the Walters
variance proxy \eqn{(\hat\theta_j - \bar\theta)^2 - s_j^2} on
\eqn{\log s_j}. Both use HC1-robust standard errors.}

\item{precision_models}{Optional precision-dependence NLLS fits to attach
for later standardization. One or both of \code{"multiplicative"} and
\code{"additive"}; pass \code{character(0)} to skip the fits and run diagnostics
only.}

\item{...}{Additional arguments reserved for future implementation.}
}
\value{
An \code{eb_diagnostic} S3 list with fields:
\describe{
\item{\code{level_test}}{Named list from the level regression with \code{intercept}, \code{coefficient}, \code{std_error}, \code{t_statistic}, \code{p_value}, \code{regressor} (\code{"log(s)"}), and \code{nobs}. Empty list \code{list()} when \code{"level"} is not requested.}
\item{\code{variance_test}}{Same shape as \code{level_test}, run on the Walters variance proxy. Empty list when \code{"variance"} is not requested.}
\item{\code{multiplicative}}{NLLS fit (legacy v1 shape: \code{psi_1}, \code{se_psi_1}, \code{psi_2}, \code{se_psi_2}, \code{r_squared}, \code{vcov}, \code{method}) when \code{"multiplicative"} is in \code{precision_models}; otherwise \code{NULL}.}
\item{\code{additive}}{Same shape as \code{multiplicative} but for the additive model; \code{NULL} when not requested.}
\item{\code{conclusion}}{Character scalar summarising the test outcomes (e.g. \code{"level dependence detected; no strong evidence of variance dependence"}).}
}
}
\description{
Tests whether estimates \eqn{\hat\theta_j} systematically depend on their
standard errors \eqn{s_j} and (optionally) fits the additive and
multiplicative precision models that downstream \code{\link[=eb_standardize]{eb_standardize()}} would
consume. Combines two HC1-robust diagnostic regressions with optional NLLS
precision fits in a single call.
}
\details{
Walters Ch 2.6 (eq. 55) motivates the level test
\eqn{E[\hat\theta_j \mid s_j] = \beta_0 + \beta_1 \log s_j}; significance of
\eqn{\beta_1} indicates that estimate magnitudes depend on precision.
Walters Ch 2.7 develops the variance proxy regression
\eqn{E[(\hat\theta_j - \bar\theta)^2 - s_j^2 \mid s_j] = \gamma_0 + \gamma_1 \log s_j}
as evidence for prior-variance heteroskedasticity. Both regressions use
HC1-robust standard errors (Stata-default convention) so reported p-values
are heteroscedasticity-consistent. The optional NLLS fits provide
ready input to \code{\link[=eb_standardize]{eb_standardize()}} without re-fitting; their \code{psi_1},
\code{psi_2}, \code{r_squared} fields are stable across v2.0-v2.5 (see
\code{\link[=precision_fit]{precision_fit()}}).
}
\section{Decision tree -- what the conclusion means}{

\itemize{
\item Level test significant only -> multiplicative model: \deqn{\hat\theta_j = \exp(\psi_1 + \psi_2 \log s_j) \cdot r_j.}
\item Variance test significant only -> additive model: \deqn{\hat\theta_j = \psi_0 + s_j^{\psi_2} \cdot r_j.}
\item Neither significant -> no standardization needed; pass \code{precision_model = "none"} to \code{\link[=eb]{eb()}}.
\item Both significant -> start with the multiplicative fit; compare its R-squared against the additive fit and pick the larger.
}
Inspect \code{result$conclusion} for the human-readable summary;
\code{multiplicative$r_squared} and \code{additive$r_squared} give the comparison
numbers.
}

\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = krw_firms$theta_hat_race,
  s         = krw_firms$se_race,
  unit_id   = krw_firms$firm_id
)

diag_fit <- eb_diagnose(
  est,
  tests            = c("level", "variance"),
  precision_models = c("multiplicative", "additive")
)

diag_fit$conclusion
diag_fit$level_test$p_value
precision_fit(diag_fit, model = "multiplicative")$r_squared

}
\seealso{
\code{\link[=eb_standardize]{eb_standardize()}}, \code{\link[=precision_fit]{precision_fit()}}, \code{\link[=eb_input]{eb_input()}}, \code{\link[=eb]{eb()}},
\code{\link[=tidy.eb_diagnostic]{tidy.eb_diagnostic()}}, \code{\link[=glance.eb_diagnostic]{glance.eb_diagnostic()}}

Other eb_diagnostic: 
\code{\link{precision_fit}()}
}
\concept{eb_diagnostic}
