% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{eb_mse}
\alias{eb_mse}
\title{Compare MSE before and after shrinkage}
\usage{
eb_mse(posterior, theta_true = NULL)
}
\arguments{
\item{posterior}{An \code{eb_posterior} object.}

\item{theta_true}{Optional numeric vector of true latent effects, aligned
with the posterior rows. If omitted, the function reports the proxy
summary instead of an exact latent-truth MSE.}
}
\value{
A named list with five numeric scalars:
\describe{
\item{\code{mse_raw}}{Truth-branch \eqn{\overline{(\hat\theta_j - \theta_j)^2}} or proxy-branch \eqn{\overline{s_j^2}}. Never \code{NA} for valid input.}
\item{\code{mse_posterior}}{Truth-branch \eqn{\overline{(\tilde\theta_j - \theta_j)^2}} or proxy-branch \eqn{\max(\hat\sigma_\theta^2 - \widehat{\mathrm{Var}}(\tilde\theta_j), 0)}. Floored at 0 in proxy branch by design. Never \code{NA}.}
\item{\code{reduction}}{\code{1 - ratio}. Can be negative if posterior MSE exceeds raw MSE (rare; mostly with mis-specified priors).}
\item{\code{ratio}}{\code{mse_posterior / mse_raw}.}
\item{\code{mean_squared_adjustment}}{\eqn{\overline{(\hat\theta_j - \tilde\theta_j)^2}}; branch-independent shrinkage magnitude. Never \code{NA}.}
}
}
\description{
Compares raw-estimate and posterior-mean mean squared error following
Walters Ch 2.5. With known latent truth, returns the exact empirical MSE;
with \code{theta_true = NULL}, returns the Walters-style proxy summary used in
the package's replicated discrimination workflow.
}
\details{
Implements the MSE comparison of Walters Ch 2.5. Two branches:

\itemize{
\item Truth branch (\code{theta_true} supplied): \deqn{\mathrm{mse\_raw} = \overline{(\hat\theta_j - \theta_j)^2}, \quad \mathrm{mse\_posterior} = \overline{(\tilde\theta_j - \theta_j)^2}.}
\item Proxy branch (\code{theta_true = NULL}): \deqn{\mathrm{mse\_raw} = \overline{s_j^2}, \quad \mathrm{mse\_posterior} = \max\!\bigl(\hat\sigma_\theta^2 - \widehat{\mathrm{Var}}(\tilde\theta_j),\ 0\bigr),} where \eqn{\widehat{\mathrm{Var}}} is the Bessel-corrected sample variance (\code{stats::sd()^2}).
}

The \code{max(..., 0)} floor is intentional: it prevents the proxy from going
negative when posterior-mean dispersion exceeds the fitted prior variance.
The proxy branch assumes posterior means, measurement-error SDs, and prior
variance are all interpretable on the same output scale; it is intended
for the replicated Walters discrimination summaries.

\code{mean_squared_adjustment} (\eqn{\overline{(\hat\theta_j - \tilde\theta_j)^2}})
is reported in BOTH branches and measures shrinkage magnitude regardless
of branch.
}
\examples{
data("krw_firms", package = "ebrecipe")

fit <- eb(
  x = utils::head(krw_firms$theta_hat_race, 120),
  s = utils::head(krw_firms$se_race, 120),
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)

post <- eb_shrink(fit$estimates, fit$prior, method = "linear")
eb_mse(post)

}
\seealso{
\code{\link[=eb_reliability]{eb_reliability()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_shrink_conditional]{eb_shrink_conditional()}},
\code{\link[=eb]{eb()}}

Other eb_posterior: 
\code{\link{eb_posterior_grid}()},
\code{\link{eb_reliability}()},
\code{\link{eb_shrink}()},
\code{\link{eb_shrink_conditional}()}
}
\concept{eb_posterior}
