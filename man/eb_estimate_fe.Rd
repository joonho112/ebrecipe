% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-fe.R
\name{eb_estimate_fe}
\alias{eb_estimate_fe}
\title{Estimate unit fixed effects and their standard errors from micro-data}
\usage{
eb_estimate_fe(
  formula,
  data = NULL,
  vce_matrix = NULL,
  se_method = c("analytical", "bootstrap"),
  n_boot = 200L,
  na.action = na.omit,
  ...
)
}
\arguments{
\item{formula}{Two-part formula \code{outcome ~ covariates | unit_id}. The
estimation path rewrites this internally as \verb{0 + unit indicators + covariates}, so returned coefficients are unit effects (not deviations
from a reference). Use \code{1}, \code{0}, or \code{-1} on the right-hand side for a
unit-dummies-only fit. In import mode (\code{vce_matrix} supplied), the
left-hand side names the precomputed unit-effect column.}

\item{data}{Data frame. In estimation mode: micro-level (student/observation)
rows. In import mode: one row per unit with \code{outcome} holding the
externally estimated unit effect and any extra columns carried forward as
covariates.}

\item{vce_matrix}{Optional \eqn{J \times J} numeric variance-covariance
matrix of the imported unit effects. When supplied, the function skips
\code{lm()} entirely and uses \code{sqrt(diag(vce_matrix))} as the standard errors.
Diagonal must be finite and non-negative.}

\item{se_method}{Character scalar; standard-error source. Currently only
\code{"analytical"} is implemented; \code{"bootstrap"} is reserved.}

\item{n_boot}{Integer number of bootstrap draws (reserved; unused while
\code{se_method = "analytical"}).}

\item{na.action}{Function applied to drop missing values prior to fitting,
e.g. \code{na.omit}. Default \code{na.omit}.}

\item{...}{Reserved for future arguments.}
}
\value{
An \code{eb_estimates} object with \code{source = "unit_fe"} and the
following public fields:
\describe{
\item{\code{theta_hat}}{Numeric vector -- unit fixed effects \eqn{\hat\theta_j}, one per unit. Never \code{NA} (estimation errors out if any unit's effect is unidentified).}
\item{\code{s}}{Numeric vector -- analytical standard errors from \code{vcov(lm)} (estimation mode) or \code{sqrt(diag(vce_matrix))} (import mode). Never \code{NA}.}
\item{\code{unit_id}}{Vector of unit identifiers in the order returned. Never \code{NA}.}
\item{\code{n}}{Integer vector -- per-unit row counts in estimation mode; \code{NULL} in import mode (per-unit sample sizes are not recoverable from a precomputed VCE alone).}
\item{\code{covariates}}{Data frame or \code{NULL} -- non-excluded unit-level columns in import mode; \code{NULL} in estimation mode.}
\item{\code{source}}{Character scalar -- always \code{"unit_fe"}.}
\item{\code{description}}{Character scalar -- records the mode (e.g. that the effects came from a pooled regression).}
}
}
\description{
Fits a single pooled \code{lm()} with one indicator per unit (or imports
precomputed unit effects with an external variance-covariance matrix) and
returns the unit-level point estimates \eqn{\hat\theta_j} and analytical
standard errors \eqn{s_j} packaged as \code{eb_estimates}. Canonical value-added
(VAM) entry point: one regression, one SE per unit, ready for the EB
stages.
}
\details{
This wrapper enforces the EB input contract \eqn{\hat\theta_j \sim
N(\theta_j, s_j^2)} (Walters Ch 2.1 eq. 8) by construction: a pooled OLS
with unit dummies produces independent (asymptotically) normal coefficients,
and \eqn{s_j} is the corresponding diagonal of the analytical VCE. The
design is intentionally narrower than a general regression wrapper -- it
returns only what the EB stages need.

Estimation mode fits one \code{lm()} over all observations with \verb{0 + unit indicators + covariates}. The unit dummies are absorbed via a synthetic
factor; covariates appear shared across units. If any unit's coefficient is
\code{NA} (e.g., separation), the function errors -- partial shrinkage on a
partially identified vector is not supported in the public path. See
Walters Ch 2.2 (eq. 5-7) for the value-added regression setup.

Import mode is for the common workflow where unit effects come from a
richer external estimator (clustered SEs from \code{fixest}, FE from a Stata
table, etc.) and you only need to wrap them. No refit happens; the diagonal
of \code{vce_matrix} becomes \eqn{s_j^2}. Off-diagonal correlations are ignored
by the EB stages but preserved on the object for diagnostics.

Robust, clustered, and bootstrap SE paths are not exposed in the current
public path; for those, compute the VCE externally and pass it via
\code{vce_matrix}.
}
\section{Decision tree -- when to use which input wrapper}{

\itemize{
\item Use \code{\link[=eb_estimate_fe]{eb_estimate_fe()}} for unit fixed-effect VAM workflows from micro-data.
\item Use \code{\link[=eb_estimate_groups]{eb_estimate_groups()}} for one slope coefficient per group (e.g., per-firm treatment effect).
\item Use \code{\link[=eb_input]{eb_input()}} when \eqn{\hat\theta_j, s_j} were computed outside \code{ebrecipe}.
\item Use \code{\link[=eb_simulate]{eb_simulate()}} for synthetic VAM data with known truth.
}
}

\section{Modes}{

\describe{
\item{Estimation mode (default)}{Fit one pooled linear regression with
unit indicators and optional shared covariates. Triggered when
\code{vce_matrix} is \code{NULL}.}
\item{Import mode}{Wrap externally estimated unit effects together with
an externally supplied variance-covariance matrix. Triggered when
\code{vce_matrix} is supplied; in this case \code{eb_estimate_fe()} does NOT
refit a regression and uses \code{sqrt(diag(vce_matrix))} as the standard
errors.}
}
}

\examples{
# Estimation mode: fit unit FE on the bundled VAM micro-data.
data("vam_simulated", package = "ebrecipe")
est <- eb_estimate_fe(y ~ x | school_id, data = vam_simulated)
length(est$theta_hat)
head(est$s)

# Import mode: wrap externally estimated unit effects + VCE matrix.
import_data <- data.frame(
  theta_hat = c(0.10, -0.05, 0.20),
  school_id = c("a", "b", "c"),
  charter   = c(TRUE, FALSE, TRUE)
)
imported <- eb_estimate_fe(
  theta_hat ~ 1 | school_id,
  data       = import_data,
  vce_matrix = diag(c(0.04, 0.09, 0.16))
)
imported$theta_hat
imported$s

}
\seealso{
\code{\link[=eb_input]{eb_input()}}, \code{\link[=eb_estimate_groups]{eb_estimate_groups()}}, \code{\link[=eb_simulate]{eb_simulate()}},
\code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_vam]{eb_vam()}}, \code{\link[=eb]{eb()}},
\code{\link[=tidy.eb_estimates]{tidy.eb_estimates()}}, \code{\link[=glance.eb_estimates]{glance.eb_estimates()}}

Other eb_estimates: 
\code{\link{eb_estimate_groups}()},
\code{\link{eb_input}()},
\code{\link{eb_simulate}()},
\code{\link{eb_standardize}()}
}
\concept{eb_estimates}
