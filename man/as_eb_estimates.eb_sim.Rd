% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coerce.R
\name{as_eb_estimates.eb_sim}
\alias{as_eb_estimates.eb_sim}
\title{Coerce an \code{eb_sim} to an \code{eb_estimates}}
\usage{
\method{as_eb_estimates}{eb_sim}(x, ...)
}
\arguments{
\item{x}{An \code{eb_sim} object from \code{\link[=eb_simulate]{eb_simulate()}}.}

\item{...}{Reserved for future use.}
}
\value{
An \code{eb_estimates} object suitable for \code{\link[=eb_deconvolve]{eb_deconvolve()}} /
\code{\link[=eb_shrink]{eb_shrink()}} downstream.
}
\description{
Aggregates the per-student observations in \code{sim$students} to per-school
summary statistics: \code{theta_hat} is the school-mean of \code{y}, \code{s} is the
school-level standard error (\code{sd(y) / sqrt(n_students)}). The
\code{sim$schools} data.frame supplies grouping metadata (\code{charter}, \code{group}).
The DGP slot (\code{sim$dgp}) is intentionally \strong{dropped} from the coercion
output — it remains accessible on the original \code{sim} object.
}
\section{Known limitation}{

The current default uses the naive school mean
(\verb{theta_hat = mean(y) per school}; \code{s = sd(y) / sqrt(n_students)}).
For unbalanced simulations where school assignment depends on \code{x}
(e.g., utility-driven assignment in \code{\link[=eb_simulate]{eb_simulate()}} non-balanced
designs), this carries composition bias — the school mean of \code{y}
differs systematically from the underlying \code{theta_school}. A future
\verb{estimator =} argument with \code{"school_mean"} (default, current
behaviour) and \code{"engine_fe"} (delegating to \code{\link[=eb_estimate_fe]{eb_estimate_fe()}}) is
planned for Phase 6 / v2.1. See the project documentation.
}

