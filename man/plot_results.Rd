% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-wrappers.R
\name{plot_results}
\alias{plot_results}
\title{Plot a compact EB results dashboard}
\usage{
plot_results(
  x,
  characteristic = "estimate",
  posterior = NULL,
  estimates = NULL,
  density = NULL,
  scale = c("theta", "r"),
  prior_binwidth = NULL,
  posterior_binwidth = NULL,
  trim = TRUE,
  annotate_prior = FALSE,
  forest_k = 1.96,
  combine = c("patchwork", "list"),
  title = "EB results"
)
}
\arguments{
\item{x}{An \code{eb_fit} object, or an \code{eb_prior} object when \code{posterior} and
\code{estimates} are supplied separately.}

\item{characteristic}{Length-one label for the empirical characteristic.
Use companion labels such as \code{"white"} or \code{"male"} when reproducing the
Walters discrimination figures.}

\item{posterior}{Optional \code{eb_posterior}/posterior data frame. Defaults to
\code{x$posterior} when \code{x} is an \code{eb_fit}.}

\item{estimates}{Optional \code{eb_estimates} object or estimate vector used in
the prior histogram. Defaults to \code{x$estimates} when \code{x} is an \code{eb_fit}.}

\item{density}{Optional mixing-density override for the posterior overlay.
Defaults to the extracted prior.}

\item{scale}{Scale for the prior panel: \code{"theta"} or \code{"r"}. When omitted,
the scale of the extracted \code{eb_prior} is used.}

\item{prior_binwidth}{Optional prior-panel histogram bin width.}

\item{posterior_binwidth}{Optional posterior-overlay histogram bin width.}

\item{trim}{Whether companion theta-scale density trimming is applied.}

\item{annotate_prior}{Whether to show prior-panel moment annotations.}

\item{forest_k}{Width multiplier for the forest plot intervals.}

\item{combine}{\code{"patchwork"} returns a combined dashboard; \code{"list"} returns
named component plots.}

\item{title}{Optional dashboard title.}
}
\value{
A patchwork dashboard or a named list of ggplot objects.
}
\description{
Combines the estimated prior, posterior overlay, and observed-estimate
forest plot into a one-row dashboard. The first two panels delegate to the
companion-quality plot helpers; the forest panel uses the existing
\code{autoplot.eb_estimates()} method.
}
\details{
Dashboards are workflow diagnostics built from live objects. They should
remain targetless; exact Lane A companion parity uses the specialized plot
helpers directly with protected \code{target_id} values and matching source
receipts.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("krw_firms", package = "ebrecipe")
krw <- utils::head(krw_firms, 40)
fit <- eb(
  x = krw$theta_hat_race,
  s = krw$se_race,
  unit_id = krw$firm_id,
  method = "linear",
  control = eb_control(standardize = FALSE, precision_model = "none")
)
panels <- plot_results(fit, characteristic = "white", combine = "list")
names(panels)
\dontshow{\}) # examplesIf}
}
