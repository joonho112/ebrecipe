% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typed-accessors.R
\name{selected_units}
\alias{selected_units}
\title{Extract selected unit IDs from an EB classification or fit}
\usage{
selected_units(x, ...)
}
\arguments{
\item{x}{An \code{eb_classification} or \code{eb_fit} object. The accessor reads the
named slot first and falls back to \code{attr(x, "selected_units")} for
v1-shaped objects, emitting a soft-deprecation message when \code{lifecycle}
is installed. Other classes raise a typed-class error.}

\item{...}{Reserved for future use; currently unused.}
}
\value{
A character vector of selected unit IDs.
\describe{
\item{Length}{Equal to \code{sum(x$selected)}; \code{0} when no units are selected, \code{length(x$selected)} when all are.}
\item{Type}{\code{character} when an explicit \code{unit_id} slot or \code{attr(x, "unit_ids")} is present; otherwise an integer-position vector for legacy v1-shape objects without \code{unit_id} (with a \code{lifecycle::deprecate_soft()} notice).}
\item{NA rule}{Never injects \code{NA}; missing-input objects return \code{character(0)}.}
}
}
\description{
v2 typed accessor that returns the identifiers (e.g. firm IDs, school IDs)
of units flagged as \code{selected = TRUE} by an \code{eb_classification} rule. For
\code{eb_fit} objects, the generic delegates to the embedded \code{classification}
slot. Replaces the v1 \code{cls$unit_id[cls$selected]} indexing pattern with a
typed, class-dispatched accessor.
}
\details{
v2-NEW typed accessor per redesign Step 2.5. Methods are dispatched on
the input class: \code{selected_units.eb_classification} reads the \code{selected}
logical mask and the \code{unit_id} slot from the constructor;
\code{selected_units.eb_fit} delegates to the embedded \code{classification}. The
default method raises a typed-class error.

For v1-shaped objects that stored selection as \code{attr(x, "selected_units")},
the \code{eb_fit} method reads the attribute with a soft-deprecation notice.
v2.5 upgrades this to \code{deprecate_warn()} per redesign Ch 12 J.9; v3.0
removes the \code{attr()} fallback entirely. Direct \code{attr()} reads in user code
remain silent in v2.0 because \code{attr()} is a base-R generic and cannot be
intercepted.

Walters Ch 3.4 eq. 103 (q-value rule) and Ch 3.5 (posterior-mean rule)
produce the underlying \code{selected} mask via \code{\link[=eb_classify]{eb_classify()}}.
}
\examples{
data("krw_firms", package = "ebrecipe")

fit <- eb(
  x = krw_firms$theta_hat_race,
  s = krw_firms$se_race,
  unit_id = krw_firms$firm_id,
  method = "linear",
  output = "posterior",
  control = eb_control(standardize = FALSE, precision_model = "none")
)
post <- eb_shrink(fit$estimates, fit$prior, method = "linear")
cls <- eb_classify(
  estimates = fit$estimates,
  posterior = post,
  method = "qvalue",
  frontier = FALSE
)

head(selected_units(cls))
length(selected_units(cls))

}
\seealso{
\code{\link[=eb_classify]{eb_classify()}}, \code{\link[=eb_pi0]{eb_pi0()}}, \code{\link[=eb_rank]{eb_rank()}}, \code{\link[=eb_test]{eb_test()}},
\code{\link[=precision_fit]{precision_fit()}},
\code{\link[=tidy.eb_classification]{tidy.eb_classification()}}, \code{\link[=autoplot.eb_classification]{autoplot.eb_classification()}}

Other eb_classification: 
\code{\link{eb_classify}()},
\code{\link{eb_pi0}()},
\code{\link{eb_rank}()}
}
\concept{eb_classification}
