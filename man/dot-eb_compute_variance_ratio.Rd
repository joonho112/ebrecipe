% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-np-output.R
\name{.eb_compute_variance_ratio}
\alias{.eb_compute_variance_ratio}
\title{Compute the per-unit posterior variance ratio for the NP path}
\usage{
.eb_compute_variance_ratio(weights, support, s)
}
\arguments{
\item{weights}{J x M numeric matrix of row-normalized posterior weights
(output of \code{.eb_posterior_weights()}).}

\item{support}{Length-M numeric vector of grid support points
(matches \code{prior$support}).}

\item{s}{Length-J numeric vector of standard errors aligned with the rows
of \code{weights}.}
}
\value{
Length-J numeric vector of variance ratios on the same scale as
\code{s} and \code{support}.
}
\description{
For each unit j, the ratio is
\deqn{V_j^* / s_j^2 = (E[\theta^2 \mid Y_j] - (E[\theta \mid Y_j])^2) / s_j^2,}
computed on the same support / SE scale the frozen engine helpers operate on.
The ratio is not clipped: values exceeding 1 are admissible and arise when
the prior is non-Gaussian (see Worksheet B.1).
}
\keyword{internal}
