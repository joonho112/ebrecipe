% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-grid.R
\name{eb_posterior_grid}
\alias{eb_posterior_grid}
\title{Evaluate posterior decision surfaces on a theta-s grid}
\usage{
eb_posterior_grid(estimates, prior, units = NULL, grid = NULL, ...)
}
\arguments{
\item{estimates}{An \code{eb_estimates} object.}

\item{prior}{An \code{eb_prior} object that carries standardization metadata in
\code{prior$spline_info} (typically from a standardize -> deconvolve workflow).}

\item{units}{Optional subset of unit indices from \code{estimates}. Ignored
when \code{grid} is supplied.}

\item{grid}{Optional override grid. When supplied, the first two columns
are interpreted as theta-scale \eqn{\hat\theta} and \eqn{s} values
regardless of column names.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
A base data frame with six columns:
\describe{
\item{\code{.theta_hat}}{Theta-scale evaluation points.}
\item{\code{.s}}{Theta-scale standard errors.}
\item{\code{.posterior_mean}}{Nonparametric posterior mean on the theta scale (residual-scale NP, then back-transformed). Primary column.}
\item{\code{.posterior_mean_linear}}{Linear shrinkage on the theta-scale grid directly.}
\item{\code{.posterior_mean_linear_alt}}{Linear shrinkage on the residual scale, then back-transformed.}
\item{\code{.p_value}}{Upper-tail normal reference \eqn{1 - \Phi(\hat\theta_j / s_j)}; screening statistic only.}
}
}
\description{
Evaluates posterior summaries on a collection of observed
\eqn{(\hat\theta, s)} pairs. Each row is an independent decision-surface
evaluation point; this is NOT a posterior density grid over latent
\eqn{\theta}.
}
\details{
Implements the decision-surface contract of Walters Ch 3.3. The
MATLAB-matching workflow is:

\enumerate{
\item Read the supplied theta-scale grid (or fall back to \code{estimates$original_theta_hat}/\code{original_s}).
\item Use \code{prior$spline_info} to transform the grid to the residual scale via \code{\link[=eb_standardize]{eb_standardize()}}'s mapping.
\item Compute the nonparametric posterior mean on the residual scale (Walters Ch 5 eq. 8).
\item Back-transform the posterior mean to the theta scale.
}

For standardized \code{estimates} with \code{grid = NULL}, the function automatically
falls back to \code{original_theta_hat}/\code{original_s}, so the public grid is
always interpreted on the original \eqn{\theta} scale. If \code{grid} is
supplied, \code{units} is ignored.

Output columns have distinct meanings:

\itemize{
\item \code{.posterior_mean} -- nonparametric posterior mean, computed on the residual scale and back-transformed to \eqn{\theta}. PRIMARY column.
\item \code{.posterior_mean_linear} -- method-of-moments linear shrinkage applied directly on the theta-scale grid.
\item \code{.posterior_mean_linear_alt} -- method-of-moments linear shrinkage applied on the residual scale and then back-transformed (consistency check against \code{.posterior_mean_linear}).
\item \code{.p_value} -- upper-tail normal reference \eqn{1 - \Phi(\hat\theta_j / s_j)}, included as a SCREENING statistic only, NOT a posterior probability or q-value.
}
}
\examples{
data("krw_firms", package = "ebrecipe")

est <- eb_input(
  theta_hat = utils::head(krw_firms$theta_hat_race, 80),
  s = utils::head(krw_firms$se_race, 80)
)

\donttest{
# Heavy: requires eb_deconvolve() (~1-3 s on 80 firms with grid_size = 100).
diag_fit <- eb_diagnose(est, precision_models = "multiplicative")
std_est <- eb_standardize(est, model = "multiplicative",
                          diagnostic = diag_fit)
prior_r <- eb_deconvolve(std_est, grid_size = 100, penalty = "none")

grid_out <- eb_posterior_grid(
  estimates = std_est,
  prior = prior_r,
  grid = data.frame(
    theta_hat = c(0.00, 0.05, 0.10),
    s = c(0.05, 0.08, 0.10)
  )
)

grid_out[, c(".theta_hat", ".posterior_mean", ".p_value")]
}

}
\seealso{
\code{\link[=eb_change_of_variables]{eb_change_of_variables()}}, \code{\link[=eb_shrink]{eb_shrink()}}, \code{\link[=eb_deconvolve]{eb_deconvolve()}},
\code{\link[=eb_standardize]{eb_standardize()}}

Other eb_posterior: 
\code{\link{eb_mse}()},
\code{\link{eb_reliability}()},
\code{\link{eb_shrink}()},
\code{\link{eb_shrink_conditional}()}
}
\concept{eb_posterior}
