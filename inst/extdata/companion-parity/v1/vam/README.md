# VAM Lane B Assets

VAM evidence is deliberately separated from protected companion parity assets.

The current state is Lane B: companion-style figure support and scalar evidence
may be staged here, but VAM targets do not enter the protected parity registry
until public package paths match the companion scalar contract and receive the
required promotion receipts.
