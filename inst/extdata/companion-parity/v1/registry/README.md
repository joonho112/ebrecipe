# Companion Parity v1 Registry

This directory will hold the protected target registry and version-specific
asset manifest.

The registry uses `target_id` as the strict lookup key. Aliases may be recorded
for migration diagnostics, but aliases do not create companion parity status.
